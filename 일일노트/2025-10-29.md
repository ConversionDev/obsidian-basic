## 2025-10-29
- 아마존 돌리는 서버 : AWS
- 서버 호스팅 : 사용자가 직접 서버를 구축하고 운영할 필요 없이, 호스팅 업체의 서버 컴퓨터 일부 또는 전체를 임대하여 사용하는 서비스(핫할땐 핫하고 안쓸땐 ㅈㄴ 안씀 -> 남아 도는 서버 임대해주는거)
-  java, sql은 20c 언어고 aws 21c언어니까 안맞으니까 java, sql 같은 놈들이 aws를 사용하기 위해서 맞추다 보니까 aws(현대식 자바)가 표준이됨.
-  ![[Pasted image 20251029095532.png|400]]
- 클래식 자바 = 스텐다드 자바 = JSE(Java Standard Edition)
- 스텐다드 SQL(ansi SQL),  변형된게 PostgreSQL(My SQL 등등 개많음) - 속도, 퍼포먼스가 좋음.
- https://aws.amazon.com/ko/what-is/sql-database/
- 요구사항은 두가지로 나뉨 : 커멘드(입력 수정 삭제, 데이터베이스 내용 바꾸는거), 쿼리(안바뀌는거- Select로 시작)
- 구조적(복수- 컨텍스트가 있다) 쿼리 언어, =구조체, 객체=단수 
- 구조체가 객체로 전환 될때 = 차원의 변화가 일어난다.
- 환경 : 컨텍스트(맥락) - 객체인지 구조인지는 컨텍스트(몇차원이냐)에 따라 결정된다.
- 속성 : 0차원,  속성(있긴있는데 안보임 private)+기능 = 객체 : 1차원 => 따라서 스키마를 가져야함. 
- insert 되는 순간 2차원 => 행렬(매트릭스) => 매트릭스때부터 의미를 가짐 1차원일때는 의미 없음
- List<> i = new Array(생성자)
- (인터페이스)          (클래스)
- 객체는 컨텍스트 위에 있어야한다. 컨텍스트가 뭐냐에 따라서 구조체인지 객체 인지 3차원인지 나뉨.
- 그거 결정해주는게 DB임 
- 컨텍스트를 만들어주면 구조가 됨. 
- 스키마 : 스키마는 데이터베이스에서 저장되는 데이터 구조(컨텍스트, 환경)와 제약조건(PK, FK)을 정의한것
- 인스턴스는 스키마에 따라 Db에 실제로 저장된값
- ![[Pasted image 20251029145435.png]]
-  자바에서 인스턴스 = 튜플 = row
-  스키마 = dto 인스턴스 = 리스트 = 튜플의 집합.
-  관계까지 있는게 DB임 = 릴레이션쉽 = RDBMS
- ![[Pasted image 20251029114843.png]]
- from  where select로 실행 되는데 자바에서는 제일 위에서 부터 실행되니까   from = 클래스 = 임포트 부터임  => 패키지는 껍데기임
- ProductService(부모) -> ProductServiceImpl(자식) = 자식이 fk를 가진다.  @Overide가 있으면 자식이다. where = 메소드 select = return
- `select` colum1, column2..... `from` table_name `where` `order by` colum1, colum2 .... `ASC`/`DESC`
- and 교집합(조건 2개) , or 합집함, in(조건이 3개 이상) 
- SELECT * FROM Products  
WHERE Price BETWEEN 10 AND 20;
- SELECT CustomerID AS ID  
FROM Customers;
- ![[Pasted image 20251029120530.png]]
- ![[Pasted image 20251029121343.png]]
- ![[Pasted image 20251029121348.png]]
- ![[Pasted image 20251029122011.png]]
- 조인 : 테이블 2개 group by 속성이 여러개
- ![[Pasted image 20251029144855.png]]
- ![[Pasted image 20251029145435.png]]
- 인스턴스가 자바에서는 한줄에 하나인데 왜 DB에서는 3개 인가? ex 밤 - 밤의 의미가 여러개니까 DB에서 서로 컨텍스트가 달라서 다르게 이해해야함.
- DB에서 인스턴스 = 리스트
- 튜플은 속성들로 구성된 하나의 행
- 값 하나하나에다가 쿼리를 때릴수 있음. (엑셀에 식 넣는거 마냥) 식을 넣어도 원본은 안건들고 복사본을 조지는거임.
- 스칼라 : 하나의 값(양), 백터 : 여러개의 값(양+방향) - 릴레이션 넣어줄때 생김.
- 자식에서 부모로 데이터가 감.
- 컴퓨터는 1차원만 볼수 있음(사람키 똑같다 치면 뒷통수 밖에 못봄)
- 단일행(스칼라)서브쿼리, 값 1개 반환, WHERE sal > (SELECT AVG(sal) FROM emp) 
- 
- ![[Pasted image 20251029153419.png]]
- 테이블에 정형화되어서 보여지는건 뷰(가짜), 쉘 드가고 염병하는거 그게 찐테이블임.-> 현장에서 일할때는 테이블이라함.
- SELECT name, sal
FROM (
    SELECT name, sal, deptno
    FROM emp
    WHERE sal > 3000
) AS sub
WHERE deptno = 10; from뒤가 뷰니까 뷰안에 또 새로 쿼리 만들었으니까 인라인 뷰임) AS 생략 
- (중첩) 서브쿼리 -> 별말없으면 기본 서브쿼리랑 이름은 똑같지만 서브쿼리로 해줌.
- SELECT name, sal
- FROM emp
- WHERE deptno = (
    SELECT deptno
    FROM dept
    WHERE loc = (
        SELECT loc
        FROM dept
        WHERE dname = 'SALES'
    )
- );
- 서브 쿼리의 종류는 스칼라, 인라인뷰, 중첩 서브쿼리
- 
- 
- 
- 