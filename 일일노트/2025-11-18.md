- 컨텍스트, 컨테이너, 컴포넌트(스프링 부트, 리액트, SQL X - 컨서레엔 -> 독립된 객체여야 하는데 SQL에는 없음 그래서 그일을 자바가 해줌.) 
- AOP 트랜잭션으로 엮어서 선(1차원)이 많아짐.  -> 컴포넌트가 여러개가 되서 2차원(면)이 되고 컨테이너로 넘어가더라.
- 면이 여러개가 되고 3차원이 되서 정육면체(박스처럼 되고) 이게 컨텍스트임
- 스레드 : 컨텍스트와 컨텍스트 잇는거. (컨텍스트에 국한되는게 아니라, 컨테이너 , 컴포넌트도 같음)
- ACC, PSC는 컨테이너임
- 0차원(Controller, Service ....)일때는 아무 의미 없지만 다 연결 되서 1차원 일때 의미를 가짐
- docker-compose yaml : `도커의 역할` -> 프론트와 자바 컨텍스트를 통합해줌(3차원 만들어줌) , 컴포넌트를 모아서 `컨테이너로 만들어준다` 
- Player : 컴포넌트 
- 
- 텐서 : 포석의 개념. 스칼라(0차원의 텐서), 벡터(1차원 - 선형)=컴포넌트, 행렬(2차원) = 컨테이너, 컨테이너부터 기능을 시작함. 3차원이상(큐브)을 텐서로 flow
- 딥러닝  : 텐서 간의 연산
- 아직 3차원까진 안가고 2차원(컨테이너)으로 연산할거임
-  ![[Pasted image 20251118104022.png]]
- UUID(Universally Unique Identifier) :  전 세계적으로 고유하도록 설계된 128비트 숫자(주민등록번호) - 유니크한값
- 테이블 넣으면 인덱스가 붙는데 그 각각의 인덱스에 유니크한 값을 붙일 수 있음 -> 식별할 수 있음 -> 객체
- player와 schedule 전체를 식별할 수 있음 -> 컨테이너(Soccer) 컨테이너 ID player(아티펙트ID), 
- java\com\labzang\(도메인) id, api(서브도메인) ,id = 컨텍스트  차원에 따라 도메인이 컨텍스트의 집합. 
- 결론 : 차원에 따라서 그룹아이디와 아티펙트 아이디가 달라짐.
- uri - 식별자(컨테이너ID), 아이디(Long id), url - 위치 -> 차원에 따라 다르게 부름 컴포넌트 
- 그룹 id : 아티팩트 id
- url : 도메인(컨텍스트들) 
- ![[Pasted image 20251118111530.png]]
- uri는 전체로 포함하는 개념. 컨텍스트로 쪼개면 uri, lb줘야함 api/컨텍스트 lb/컨테이너 @id/컴포넌트
-  ![[Pasted image 20251118115930.png]]
-  ![[Pasted image 20251118120437.png]]
- ui는 볼륨이 없다.
- 
- 컨텍스트, 컨테이너, 컴포넌트 (자바 @id, 타입스크립트 @)
- ![[Pasted image 20251118134127.png]]
- 리액트 어플리케이션 : 리액트(프) + context(백) => 2개가 합쳐질때 서버사이드 개념이나옴
- 프론트에서만 이야기하면 라이브러리 
- 라우팅 - 데이터나 요청을 목적지까지 적절히 전달
- ![[Pasted image 20251118134159.png]]
- 리액트의 특징
- 가상돔 : 화면 뜰때만 존재
- 단방향 데이터 전달
- 컴포넌트 지향/함수컴포넌트(FC)
- 플럭스(flux) 아키텍처와의 친화성
- **Redux**는 자바스크립트 애플리케이션에서 **애플리케이션 상태(state)를 예측 가능하게 관리**하기 위한 라이브러리 - 발전된 형태
- 도커의 역할 : 컨테이너 생성.
- 라우팅 : 데이터나 요청을 목적지까지 적절히 전달
- 
- ![[Pasted image 20251118133805.png]]
- ![[Pasted image 20251118133516.png]]
- 리액트에서는 컨테이너가 무조건 있어야 하니까 생략 가능
- 자바로 넘어갈 때 컴포넌트 밖을 벗어나니까 state -> props
- 리덕스 : props를 관리
- 컴포넌트 : 페이지 컴포넌트
- 이때 리덕스는 : 컨텍스트
- 
- 렌더링 : 화면을 그리는 과정 전체
- 서버사이드 렌더링 : 서버에서 HTML을 미리 만들어 보내는 방식
- 
- 특정 카테고리에 최적화된 에이전트야. ModelContextProtocol(인공지능(AI) 모델이 외부 도구나 데이터 소스와 연결될 수 있도록 돕는 개방형 표준 프로토콜)
- 
- ![[Pasted image 20251118172656.png]]
- 자식은 콜백 함수로 return 값을 받아서 데이터를 전달 할 수 있는데, 이때 부모의 상태가 변경되는 거지 데이터가 이동하는 것은 아니다. 데이터 소유권을 부모가 갖고, 자식은 상태 변경을 직접 못하고 요청만 보낸다.
- 
- 훅(Hook)은 클래스(class)를 작성하지 않고도 state와 다른 React의 기능들을 사용할 수 있게 해주는 메소드
- 

