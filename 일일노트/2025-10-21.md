## 9 : 20 ~ 10 : 30
- ; 식,문 -> 알고리즘
- 클래스 밖 : 함수(엑셀)
- 클래스 안 : 메소드 => 파이썬은 다 있고 자바는 메소드만 있음
- 클래스 밖에 두면 컴퓨터가 알고리즘을 처리(process-cpu가 작업하고 있다)
- CPU(Central Processing Unit=processor)
- 메모리가 작동하고 있다 - loading
- 디스크가 작동하고 있다 = 저장
- 입출력 // 기억, 연산
- 입력 : System.in, 출력 : System.out
- 메모리와 디스크 차이 : 휘발성, 비휘발성.
- CPU(Central Processing Unit=processor) 
- `제어`, (control)
- 분기 (if와 switch) - Branch => 깃허브 팀원들 한테 나눠주는거.
- 반복(for, while) - loop
- `연산`(처리, operation) cpu, ram, disk
- 사칙연산이 +의 변환임(-,%,X,= = ) =>산술 연산자
- = 할당연산자 : 변수에 값을 담아라(assignment)
- `cpu = 연산과 저장(디스크,메모리) = 메모리(연산과 제어) =연산(산술, 할당)`
- 
- 제어(Branch와 loop) , 브랜치는 if와 switch(등호 있으면)
- 대입 = 할당 = 연산= 저장 = assignment
- 함수 : 알고리즘에 이름을 붙인것.
- 메소드 : 클래스 안에 있는 것
- call(콜) : 함수를 호출 한다.
- 수열, 문자열, 배열, 행렬(비선형=Metrix 1차원 선 2차원 면 : 2차원 이상 - dimension 3D)
- 수열 : Element가 기계어면
- 배열 : Element가 자연어(문자열) 
- element가 반복되는 거니까 2차원(loop)
- if,switch, for, while 
- loop counter(배열)


## 10 : 30 ~ 11 : 20
- stop의 주도권이 프로그램에 있으면 for(코노에서 돈 넣으면 3곡 할수 있는거)
- stop의 주도권이 사용자에게 있으면 while(게임 그만 하고 싶을때 그만함)
- 무한루프는 와일에서만 이루어짐.
- 네비게이션 바 : 가위바위보 계속 하시겠습니까 
- ![[Pasted image 20251021111624.png]]
- express(고속도로 영어로 Expressway) = 빠르다 = `식`이 문보다 먼저 처리됨.
- `if,switch, for, while`가 걸리는 순간 ();여도 메소드가 아니라 `문`이다.

## 11 : 30 ~ 12 : 20
![[Pasted image 20251021113824.png]]
- 스퀘어(배열) [ ] 들 = 메모리에 1,2,3을 저장해라 = 확장 for문 = for each 문
- in 안 ![[Pasted image 20251021115352.png]]
- 전체 배열 안에 키가 아닌 값이 인스턴스가 아니라 element
- 스키마 : 컬럼(세로)의 집합 , map안쓰고 dto 넘어간 이유
- map 진화된게 dto => dto도 map 구조다.

## 12 : 30 ~ 13 : 20
실습

## 14 : 30 ~ 15 : 20
- 스프링 이니셜라이저 : <u>마이크로 서비스 개발</u> => 서비스가 중요하다 => 마이크로니까 서비스가 여러개 있다.
- 자바, 그레들(빌드 도구)이 빌드한다
- @(at) Annotation : 
- Notation :  => 이야기 하다맘.
- JSON : JavaScript Object <u>Notation</u>
- @PathVariable : 경로에 변수 주는 것.
- POJO = Plain Old Java Object : 플레인 요거트 별맛 안남 => 자바 문법 만으로 만든 순수한 자바 객체 : 마틴 파울러 랑 스프링 만든애랑 둘이 친해서 "코딩을 패턴화해서 만들자", MVC패턴 나옴
- @Component 
- implement
- @Bean(강낭콩) : 스프링 컨테이너가 관리하는 Bean으로 등록하라
- ![[Pasted image 20251021152101.png]]
- 콩깍지로 운영하면 콩의 내용을 볼수 없으니까 은닉화 
- 내가 직접 만든 객체를 스프링이 대신 관리하게 해주는 표시 = DTO,VO Entity
- => 이거 2005년 거임, 예전거임 볼일 없음
- 
- 

## 15 : 30 ~ 16 : 20
- ![[Pasted image 20251021155316.png]]
-  Service에서 Repository에 연결시킴.
- 
- 
- 
- 
- 
![[Pasted image 20251021200850.png]]


## 16 : 30 ~ 17 : 20



## 17 : 20 ~ 18 : 20