gpt : 파운데이션 기반 쓰기까지 가능 트렌스포머는 읽기만 가능
gpt : 튜닝 X 라마 튜닝 O
모델의 집합 : 시리즈
서양 애들의 모델  
라마의 파생형 모델 : 알파카 
무엇을 훈련 시키는가? 모델(midm)을 훈련 시킴.
라마는 짐을 나르는 운송 수단, 패션 모델
짐 싣다(로딩) -> 모델에 데이터가 담김 -> 데이터 이동 -> 데이터 동선 : 정해진 동선으로만 왔다갔다함
`모델 : 프로토콜을 가지고 데이터를 담고 정해진 동선으로 이동하는 장치`
어디어디는 샤넬 입어야함 : 프로토콜 규약
학습을 화면과 왔다갔다함.
오픈 API는 튜닝이 안되서 미듬을 씀
미듬을 훈련 시킬예정.
화면, pgvector, 모델(살아있음, 생성), 레포지토리 
라마의 파운데이션 모델이 트랜스 포머고
알파카의 파운데이션 모델이 라마.
랭체인 : 모델을 담는 프레임 워크
전체는 : RAG - 체인

rag 파이프라인
= 스프링 -> mvc
라우터가 컨트롤러
모델이 렝체인
전체는 fastapi인데 랭체인에 들어가 있음.

chat_rotuer.py 서빙 - 질문 한개. 인보크, 여러개 배치
training_router.py 학습 - 스레드

async로 하는 이유 
![[Pasted image 20251218115534.png]] 관리자 모드로 사용
embedding ingest(수집, 밖에서 안으로)

통신한 거 두고(axios, fetch로 검색) 여기에 있는, 라우터 기능을 분리해서 chat_router.py로 이전해줘.

AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig는 뭐하는 거야
@app/service/chat_service.py 여기에 임포트된 클래스를 활용해서 peft의 qlora 방식으로 대화하고, 학습 하는 코드를 추가해줘

aws : 
All services - API Gateway
1. db(RDS)연결 
2. 서버(ECC=EC2) (Ram) - 코드 app쪽이 올라감
3. 디스크sss=s3 
4. 
5. 프론트(버셀) - 아마존 아님
![[Pasted image 20251218152215.png]]

우측 상단 Aisa Pacific(Seoul)로 맞춰 놔야함
검색창에 ec2 Elastic Compute Cloud
탄력있는(접속자가 많으면 복제 적으면 줄어 들게, 유연하게 처리하는것)

검색 하면 화면에 첫번째 : vertual server
Instance(Running)
(Capacity Reservations) 수업시간에 안함.
Elastic Ips -> 도메인, load balancers도 누르지 마라
snapshots(백업, 복구 가능 ) - 돈나감
Instance 클릭함.  오른쪽에 Launch 누름
Name and tags : 우리팀은 팀 이름, 난 개인이름 할거
ubuntu 클릭
하단 우측 화살표 free tier 무료가 가능한 것들 1~5
캡쳐하고 chatgpt에
mi:dm 모델을 가진 langchain + fastapi 구조에, 실시간 학습기능은 배제한 api 서버를 구축하는데, 실시간 사용자가 나밖에 없는 mvp 버전을 만들거야
Ubuntu Server 24 d4LTS 쓰래서 이거 씀 뭐 눌러서 t3.small됨
Docker 꺼지는 이유 OOM(Out Of Memory)이 발생해서.
Key pair -> create -> 키이름(kku1031)
위의 구성에서 챗봇을 만드는데, 로그인도 없는 상태야. 이 중에서 어느것을 선택할까, 다운로드 받음
아주 엄격한거 아니면RSA 그대로 
Network settings, 하단 부 캡쳐 anywehe(보안 실제
 뇨ㅜㅜ====)
뭔가 눌러서 만들고
view in

인스턴스 클릭
<사진>
쭉 내리다보면 Security가 있음
Security 누르고 security 그룹이 있음
inbound rules 화면 캡쳐 해서 gpt에 , 이 보안 그룹 설정에서 현재 내가 접속한 IP 주소에서만 접속 하는 설정을 하도록 알려줘.

ALL TCP, My IP
outbound 사진

이 fastAPI를 연결된 ec2에 github action을 통해 ci/cd 방식으로 배포 하도록 전략을 알려줘

deploy.yaml이 만들어져야함
