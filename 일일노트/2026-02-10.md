- 백엔드 : 에이전틱 레그(아키텍처 스타일), 오픈 클로 : 큰개념(설계 원칙) (보안이 약해서 물리적으로 통일, 폰으로 우리가 만든 사이트든 뭐든 통제, 개인 비서 역할로 휴대폰에서 쉽게 통제하기 위해서, 코딩은 커서가 화면은 휴대폰, speaker to text)
- ![[Pasted image 20260210105226.png]]
- HNSW: “비슷한 벡터”를 빠르게 찾는 인덱스
- B-tree: “정확한 조건”(standard_type, page, unique_id)으로 범위를 좁히는 인덱스
- 실제로 테이블 기반 검색을 쓰는 것이 맞고, ingest가 채우는 disclosures 테이블과 동일한 데이터를 검색하게 되었고, standard_type 조건도 그 테이블 컬럼으로 적용됩니다.
- 요약: 예전에는 테이블 기반이 아니라 PGVector 컬렉션(langchain_pg_embedding) 만 쓰고 있었고, 지금은 disclosures 테이블 기반 검색으로 바꿔서 그 경로를 쓰고 있는 상태

-  데이터 적재(BGE-M3) 후 올바르게 문서를 찾아 오는지 테스트
- IFRS, OECD 등 영어 원문에서 "Introduction”, “human capital”, “reporting”, “국제”, “지표” 같은 공통 표현이 많아서, “IFRS”, “OECD” 질문과도 의미적으로 가깝게 잡힘
- => 별도의 컬럼 타입을 두고 명확하게 찾아올 수 있도록 Btree 인덱스 추가
- ![[Pasted image 20260210115252.png|1000]]
- 화면에서 표 추출 전략에 따라
- PDF_STrategy.py이 추상클래스를 구현한 기준은 pdf_strategy.md 이것과 같다. Gof의 전략 패턴을 적용하려고해. pdf_enum.py md를 참조해서 (7개) 사용할 모델 
- 다이얼로그(=팝업창을 휴대폰에서는 그렇게 말함)