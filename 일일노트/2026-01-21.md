- 코일렉트리카 규칙기반이냐 정책기반이냐(글을 읽는다 리더기) 판단을 하고 규칙이면 안넘기고 정책이면 엑사온에 넘긴다. 
- 규칙기반이면 엑사온 아닌 서비스로 넘긴다.
- 실질적으로는 엑사온, 메인(라마)
- 폴더에서 agent면 정책기반 services면 규칙 기반
-  
- https://brunch.co.kr/@vsongyev/28 레그의 종류 
- 아카이빙 : 내 도메인 지식을 어떻게 이해할 수 있게 쌓을 것인가. 이것이 어려움

- 레그의 종류 : 나이브 레그, 어드벤스드 레그, 모듈러 레그
- 초창기 버전 (Naive Rag) : 순진한 ->  할루시네이션이 심하다는 단점을 가짐.
- 현재 : 모듈러 모놀리식 아키텍처 구조
- 모듈러 : 한 몸인데 칸막이(아마존에 합쳐서 올리는 것)
- MSA : 완전히 분리
-  
- 모듈형 RAG를 구축하려 해. 이 아키텍처에서 mcp로 연동하고, neon에 있는 pgvector와 랭그래프 구조로 데이터의 파이프 라인을 구축하고 싶어. 실행 과정을 알려줘
- pg백터로 임배딩하는 테이블을 추천해줘
- DDL : create(테이블관리) DML : select, join(한줄 관리)
- DDL은 생성/변경/삭제(구조), DML은 조회/삽입/수정/삭제(데이터).
-  
- DB -> Base -> BaseModel -> Json -> 화면으로
- BaseModel : Base에서 dto로 꺼낼때 사용하는 것
- sql엘케미(화학)로 base 클래스를 상속받은 구조로 하면, pgvector에 저장하려고 해. 이 때 sql앨캐미에 대해 설명해줘
- SQLAIchemy : SQL을 직접 쓰지 않고도 자동화가 가능하게 해줌. 
-  
-  사용자가 자주 쓰는 물건 추천. 로그인 하면 기본적으로 계속 추천해줌.
- product를 구입하는 consumer 스키마를 임의로 작성해줘. 교차 엔티티로 order를 둘거야
- sql엘케미(화학)로 base 클래스를 상속받은 구조로 하면, pgvector에 저장하려고 해.
- conmsumers.py 이 스키마 DDL로 만들어줘 -> 쿼리문 -> 네온에 던짐
-  
-  
- ![[Pasted image 20260121151847.png]]
- ![[Pasted image 20260121151933.png]]
- 
- ![[Pasted image 20260121154213.png]]
- 자바 : 연산을 사람이 했는데 BaseModel은 연산을 알아서함 -> 하나로 -> 싱글톤으로 만들어야함
- ![[Pasted image 20260121155359.png]]
- BaseModel을 transfer(서비스, 규칙기반- O,X로 떨어지는 것)와 state(에이전트, 정책기반)로 나뉜다 따라서 base를 2개로 나누어야함.
- models가 정책 기반 states가 규칙기반
- @app_t/domain/admin/models/consumer_model.py @app_t/domain/admin/models/order_model.py @app_t/domain/admin/models/product_model.py 여기에 @app_t/domain/admin/bases/study/consumers.py @app_t/domain/admin/bases/study/orders.py @app_t/domain/admin/bases/study/products.py 이 파일을 참조하여 파이덴틱의 basemodel을 상속 받는 transfer 클래스를 ConsumerModel 같은패턴으로 작성해줘
- 훈련받은 머신 : 상태머신
- @app_t/domain/admin/states/consumer_states.py @app_t/domain/admin/states/order_states.py @app_t/domain/admin/states/product_states.py 위와같은 맥락으로 상태머신 이 상태를 구분하는 state클래스로 생성해줘
- ![[Pasted image 20260121165758.png]]
- @app_t/domain/admin/agents/consumer_router.py 에서 들어온 명령을 @app_t/domain/admin/orchestrator/consumer_flow.py 에서 @app/artifacts/fine_tuned/koelectra 이 모델을 로드하여 규칙기반이면 @app_t/domain/admin/services/consumer_service.py로 보내고 정책기반이면 consumer_agent.py로 보내는 코드를 작성해줘.
- ![[Pasted image 20260121172221.png]]
![[Pasted image 20260121171931.png]]
- OpenMP : 여러 스레드가 **동일한 메모리 위치에 동시에 접근**하여 값을 쓰려고 할 때 발생
- 줄은 DML, 데이터는 DDL
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 
- 

