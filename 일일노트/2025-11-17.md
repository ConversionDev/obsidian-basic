- ApplicationContext = 스프링의 객체(Bean) 저장소   
- PersistenceContext = JPA의 엔티티(데이터) 저장소 
- 라이브러리 :
- 디펜던시 :
-  ![[Pasted image 20251117095403.png]]
-  ![[Pasted image 20251117095529.png]]
- 폴더 까지는 패키지라고 하고 파일까지 합치면 라이브러리임 사실 같은 말이긴함.
- 스프링데이터 JPA 쓰기전에 뭘 썻는지 뭐가 좋아졌는데
-  .env : 환경설정
- 패키지, 라이브러리, 의존성 컨텍스트(상태)가 달라짐.
- 
- groupId : 그룹의 식별자(id)  + artifactId : 인조키
- ![[Pasted image 20251117110020.png]]
-  ![[Pasted image 20251117115252.png]]
-  기존 레포지토리가 나뉨 -> 리팩토링
-  루트 그래들이 단순하고, MSA 내부의 그레들에 다 들어가는 이유 -> 루트 그레들을 상속하니까
- 
- 리액트 @컴포넌트
- 아토믹 패턴

- 3개 공통점 :  서로 다른 이기종 언어같의 데이터 교환
- @Component(@Controller, @Service, @Repository) @Entity JPA, 그래서 각각이 다 컴포넌트임
- 컴포넌트 : 
- 컨테이너 : 
- 컨텍스트 : 
- 환경 : 컨텍스트의 집합
- 
- @Component 컴포넌트 스캔 시점(main실행시), 애플리케이션 시작시 생성된 @bean이 컨텍스트에 등록
- 빈 생성 이후 해당 빈의 메서드가 호출될 때, 트랜잭션 기능이 적용된다.
- 
- 컴포넌트 : 각 언어마다 정의된게 다름, 컨테이너(도커), 컨텍스트(컨텍스트에 등록), 컨텍스트와 컨텍스트 간의 데이터 교환을 가능.
- 넥스트도 도커에 띄워서 연동.
- 
- **프로세스**: 실행 중인 프로그램 자체를 의미하며, 각 프로세스는 고유의 메모리 공간을 가집니다.
- **스레드**: 프로세스 내에서 실행되는 **하나의 실행 흐름**입니다. 하나의 프로세스는 하나 이상의 스레드를 가질 수 있습니다. -> 메소드
- **멀티 스레드**: 하나의 프로세스 내에서 여러 스레드가 **동시에** 실행되는 상태입니다.
- 
- 스레드(메소드)마다 각각 다른 트랜잭션을 할당한다.
- 영속성 컨텍스트가 다르다
- 
- 