- ![[Pasted image 20251110093829.png]]
- 자바 : 리스트, 맵(힙) = 프롬프트(커맨드: CLI) :  스택(접시, 위에꺼 먼저 씀), 큐(FIFO - 피포, 먼저 온놈이 먼저 타는거, 선입선출)//리스트, 인덱스 - 프롬프트
- 행동 : 커맨드 패턴 - 함수 호출을 “객체화” 시켜서 실행과 호출을 분리하고 실행을 나중에/큐에/취소/재실행 이 가능하게 하는 패턴 = 요청을 객체 형태로 캡슐화하는 패턴
- 만든걸 객체로 만들고 사용만 하게 하는 것 , Git git = new Git 깃을 우리가 쓸때 실제 인스턴스만 가져와서 쓰는 것 처럼 하는게 패턴.
- 인스턴스 = 객체 git add .  자바스크립트에서는 JSON
- 타입 : 스키마, 객체 : 
- <u>git</u> add . 동작앞에는 객체를 붙여라, 접점 : 인터페이스
- Node(타입) Node Package Manager(객체)  
- 환경이 타입(객체가 서있는 맨땅), 따라서 CLI 칠때 npm.sadkjkasdj 형식
- 
- 스택이 힙(서버 언어에서 자주 씀), 스택 : 힙(맵) - 무더기로 쌓아둠, 로딩 속도가 빨라짐, 가비지 컬렉터가 처리= 가비지 컬렉터가 있어야만 쓰임 // 스택 - 조심조심 놔야 되니까 느림(리스트)
- ![[Pasted image 20251110100931.png]]
- 캐시 메모리 : 상주하고 있는 메모리
- 객체는 의인화 해서 생각 하면됨, 도커 실행해!, 깃 실행해! 
- AI UI 만들거면 (React + model API 연동 + 빠른 빌드) 이 조합은 더더욱 pnpm
- pnpm 장점 : 개빠름 최신도구랑 호응 좋음, 단점 : 구형 스크립트에서 인식 못할 수 있음 
- 아무거도 안하면 next.js에서 npm으로 씀
- 
- 속성과 행동을 하나로 묶어서 구조화 했다.
- 객체 모델(도메인=DTO), 
- 어노테이션 없는 포조=객체=순수객체, 스프링은 순수객체 쓰긴하지만 순수 객체만 쓰지 않으니까 빈 객체 씀.
- dom ,  이제 이거로 감 `객체 종류(타입과 객체)`
- <!DOCTYPE html> - 도큐 먼트 타입이니까 객체가 있어야함. ->  객체의 타입은 html -> 리액트 (돔 : 타입이 html인 객체) 사용해서 객체로 만듬. -> 자바에서 인스턴스가 돔이 될거다.
- 타입이 하나밖에 없으니까 생략 가능
- 타입 일때는 값이 없는데 검색창에 검색하면 값을 가진 객체가 활성화 되고 이게 돔이 된다.
- 리액트 : 반응형(사용자에 반응함), 너 3편 안봣어 추천해줘서 짜는게 리액트, 타임리프는 이게 안됨. 
- 돔 : 실제 돔/가상의 돔 
- 가상의 돔(버추얼 돔) : 변화만 비교
- `리액트 핵심 : 상태(디자인 패턴 : 행동) 가 바뀌면 React가 알아서 화면을 갱신한다`
- JPA -> 쿼리를 직접 짬.(사실 하이버네이트가 하긴 하는데 걍 일케 말하면 됨)
- 비동기적처리 가능, 부드러운 렌더링
- 리스폰스
- ![[Pasted image 20251110115407.png]]
- 함수도 객체다, 이전까지는 클래스만 객체였는데 함수도 객체가 됨.
- 클래스파 vs 함수파 싸우다가
- 2019 코로나 생겨서 프론트 개 발전, 줌 발전, 비동기 렌더링(화면끊김 최소) 안정화
- 클래스파 vs 함수파 싸우다가 메타(함수파)가 이김.
- 리액트는 상태패턴인데 상태패턴에서 디테일하게 가면 원자로 나뉨.
- ts(typescript), tsx(expansion) : 돔
- DTO 모인거 리스트
- ![[Pasted image 20251110123401.png]]
- ![[Pasted image 20251110123853.png]]
- 리액트 : 모든 것은 컴포넌트(=객체)다, 컴포넌트(함수형)
- 자바로 따지면 name 자체가 독립적인 객체가 되서 기능함. 이제 그냥 다 객체임.
- 따라서 이름만 바꾸어도 화면이 다바뀜.
- Molecules(말리큐)
- page를 원자 분자 유기체가 없어서 나중에 쪼개서 다 만들어야 함. 지금은 안할거임
  아토믹 패턴으로 만들어줘라고 하면 알아서 다 쪼개짐.
- 노테이션이 있다 -> 객체 기능을 객체로
- 
- 리액트 - 라이브러리, next.js = 프레임워크(깡통, 파이프라인만 있음)
- 컴포넌트 : 형이상 , 아톰 처럼 생겼고, 그 밑에 타입이 형이하
- 함수안에 함수 : 훅
- 스트럭처 나가면 인스턴스 
- 나가면 컴포넌트, 엘리먼트
- ![[Pasted image 20251110145444.png]]
- Button(태그를 감싼거 원자), <button 태그이자 돔, 원자 
- APP , Button : 원자 위에 급(분자든 뭐든 etc)
- F12 누르는 순간 HTML로 상태가 바뀜. 리액트는 볼수가 없음.
- 컴포넌트=클래스 만드넹
- Anonymous(어나니머스) 객체 : 무명 이름이 없음 이름없는데도 객체임., 원래는 이름 불러줘야 객체잖슴.
- 자바도 이름 없는 어나니머스로 바꿈. 어차피 객체니까.
- 아톰 : 네임드 , 노테이션 감싼놈 => 객체
- ![[Pasted image 20251110151918.png]]
- 람다 = 에로우 펑션, ai코딩. => fat 에로우(자바스크립트랑 리액트) , -> thin 에로우
- 리액트 - 바로바로 바뀌니까 인터프리터 
- 리액트 요소(원자 - 태그) 원자 이상 분자, 템플릿 입장에서 원자는 감싸져있음.
- JSX -> TSX 로 진화
- 웹팩 :  자바스크립트 빌드 도구 
- 실제 돔 : (안바뀜), 가상돔(두페이지가 존재 할수 없음)은 바뀜
- 책, 휴대폰(SPA = **Single Page Application**)
- 상태 : 페이지가 뜨는 순간만 객체가 되고 그게 아닐떄는 객체가 아니다. 총 객체가 1개임
- (값이 투입이 될때)
- html 태그 요소를 가지고 있음 = 아톰. 
- 리액트 요소나 다른 컴포넌트를 조합한 것을 반환합니다.(아톰 이상)
- ![[Pasted image 20251110155051.png]]
- ![[Pasted image 20251110155242.png]]
-  type : 속성
-  ![[Pasted image 20251110155753.png]]
- {} 노테이션 있으면 객체. <b 실제 돔
- 펑션 (){} 컴포넌트 < tag <B 가상 돔 , 실제하는거를 감쌋으니까. return 되는 것만 봄.
- 자바는 클래스 통째로 들고 와서 쓰고 , 리액트는 필요한 부분만 바로 호출해서 쓰는거임.
- 어나니머스 = 람다
- 
- `Gof(객체지향 설계)에서의 "use 관계" - 객체간 의존 관계`
- `React에서의 "use" 접두사 - 훅의 사용 규칙 및 의미`
- 
- 자바스크립트는 호이스트 : 전역로 움직임, 리액트는 후크 : 로컬으로 움직임.
- ![[Pasted image 20251110163951.png]]
- 어떤 테이블을 쓸지 몰라서 파사드를 씀 : 파사드란 박스에 넣음 
- 원자, 분자, 유기체, 템플릿, 페이지