- ship 배를 물에 띄우다(진수하다) - 배 다 만들어 테이핑
- reliable 
- agents - 요원
- agent 제품을 완성하고 주는거
- 프레임워크에 내장된 라이브러리가 많이 있다.
- FastAPI, 체인(input박스) = 자바는 파이프라인
- https://wikidocs.net/231156
- ![[Pasted image 20251216095802.png]]
- invoke : 단일  , 질문 하나
- 배치 : 대용량 데이터(DS) 일괄 
- 컴포넌트 : 인스턴스
- Few-shot : 많지 않은 데이터
- 모델 : transformer f= fx + b = 기능 
- 랭체인의 언어 모델 : LLM
-  ![[Pasted image 20251216100322.png]]
- llm은 fastAPI가 안되니까 LLM 전용이 등장
- 자바 컨 서 모델 - 우리가 생성
- 패스트 라우터 서비스 모델 - 다른 사람거 씀
- lm에서는 모델만 갈아 끼움.
- 모델 : 라마 
- 서치 없으면 없다 환각이 없음
- 리트리버 프롬 프트 환각이 있음.
- 레그 리트리버, 강아지 강화해서 가져옴 집어오다. 
-  ![[Pasted image 20251216110921.png]]
- 쉽 : 개발 불가 관여 할수 없음, 완성시키고 주니까, 배포는 지속적으로 변화함. 
- 파일을 통째로 읽는다.
- rag = 백터 DB(형식이 없어도 읽음, PDF등도 가능)
- vertor store - 백터 DB
- Chroma(크로마)
- API에서 받아오는 것 -> JSON
- AI 모델 -> PKL(안열림 내부에서 못봄), bin, json ... etc 
- 여러 파일들을 싸잡아서 가지고 있는거 모델 
- 1세대 모델 : 직접 만듬(스프링,), 2세대 : 남이 만든거 다운 받는 거(언어모델, LM) -  패스트 API, 비전모델(바나나 등등) //2 세대 까지는 파이프라인
- 3세대 :LLM(아예 안보임) // 3세대 까지는 체인.(옷 처럼)
- 2세대는 트렌스 포머(읽기). 3세대 지피티(트렌스포머를 사용)
- 지피티와 트랜스 포머의 차이 : 생성형(쓰기)
- 1세대 : 수작업, 가내 공업, 3세대 대기업
- 1,2 세대 MVC패턴(서치엔진, 검색엔진) 3세대 RAG (시스템이름)패턴
- 체인이니까 구조는 똑같다. 디테일은 다름.
- 파이프라인 ( 한줄기 : 쓰레드 - 저장) 스레드 묶음 스트림, 쓰레드가 리트리버(save 안해도 지가 알아서 감), 스트림과 대응하는 개념 RAG
- 출처 : 크롤링 본진.
- 화면과 DB를 연결해주는 입력과 출력을 나타내는 매개체
- 작동하는거 보고 똑똑할 수 있겠다 라마
- 라마(오리지날) -> 알파카(라마 바탕으로 튜닝)가 물어옴, 로라(이미지 생성) -> 코 알파카(LLM)
- 하나는 인보크, 
- 올라마
- ![[Pasted image 20251216130322.png]]
- PromptTemplate, 화면에서 넘어온 글자를 담고 있어서 컨트롤러
- 라마모델에 담는 통 라마
- gpt의 프레임워크 랭체인, 이 다음 단계 agent
- agent에 담으면 rag를 넣어버린 서비스 변호사 agent
- rag - 렝체인 + pg 백터
- ![[Pasted image 20251216151930.png]]
- 리트리버가 들어가서 찾아오니까 여기는 null 값이 들어옴
- 
- 
- 
- 
- 

