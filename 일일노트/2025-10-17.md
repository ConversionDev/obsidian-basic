## 9 : 20 ~ 10 : 30
- 객체를 초기화 한다. object : view + 메모리(실제 물건) - instance
- 디스크, 인간 세계관에서 객체 : 클래스 => 클래스 보고 instance의 모양 판별
- 객체를 초기화 한다 -> git init -> 객체 안의 변수를(필드)를 처음 넣어 주는 과정
- 축구 -> 필드(field), 골키퍼 area(한국에는 `area` 개념 X, 아이스하키에서는 area라고 하고 골프는 필드라 함, are 벗어나도 아웃이 없음) `변수(필드)` -> 이말, `변수(area)`도 존재한다.=> "area 개념이 들어간게 곧 지역변수임" 
- ![[Pasted image 20251017095505.png|400]]
- 데이터의 전파 = 객체의 이동으로 일어남.  물통(전체 : 필드), 물 찬데까지(에어리어), 빈곳 개많음, 비어 있는 곳도 필드(에어리어라는 특수한  공간제외하면 다 필드)
-  현업에서는 에어리어를 변수라고 하고 속성을 필드값라고 함. 프로펄티(속성)
- LoginDTO loginDTO = new LoginDTO(); // 객체 (메모리에 객체의 주소를 만들어라) ->지역변수, 인스턴스고 일회용 객체 니까 쓰고 버려진거를 모아지는 곳. 가비지 콜렉터 : 더 이상 사용하지 않는 객체(쓰레기 객체)”를  
	`자동으로 메모리에서 지워주는 청소부`

- ![[Pasted image 20251017110608.png]]
- 지워도 작동 하는 값 : 디폴트 (가비지 콜렉터, 생성자도 그렇고), 생성자 메소드
- 선언 : 클래스를 선언한다.  메모리 없음, 디스크에는 존재하는데 이건 메모리 중심이니까 영토가 없는거.  -> 만든다(빌드, 런까지). 빌드까지 가는건 메모리가 없고 런까지 갈때 만들어져서 메모리에 올라감. 
- 독립 선언 : 하겠다 독립 ; 실행 하는 결과물 영토의 유무 클래스 필드 자체가 선언.
- `인스턴스가 만드는 것 : 컨스트럭터 날리는것 : 가비지 컬렉터`
- ![[Pasted image 20251017114223.png]]
 - 전체가 필드인데 이 필드를 아래쪽에 두어도 오류가 안남 -> 필드(고지대), 에어리어(저지대) 
 - 필드에서는 아무곳에 둬도 오류가 안나는데 같은 에어리어 쪽에서는 위에서 아래로 흘러감.
- 컨트롤러에 vo가 없는 이유 -> 서비스에서 출발해서 서비스에서 조짐.

![[Pasted image 20251017115720.png|600]]

-  자바데이터를 html로 보내야함 언어가 다르니까, 

## 12 : 30 ~ 13 : 20
![[Pasted image 20251017130928.png]]



## 14 : 30 ~ 15 : 20



## 15 : 30 ~ 16 : 20



## 16 : 30 ~ 17 : 20



## 17 : 20 ~ 18 : 20