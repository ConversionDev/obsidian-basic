- ![[Pasted image 20251107094309.png|600]]
- 스칼라 : 람다(점) 스트림 : 백터(선) = 어레이 리스트 = 선형 구조 = 같은애만 모아도 되고 다른에만 모아도 된다. -> 디멘션에 올라간다(차원이 올라갔다 = 은닉했다) , 면
- 리스트 :  하나는 int 하나는 String, 어레이 : String
- aggregation(백터 -> 스칼라), 리스트 구조의 내부의 값을 하나의 값으로 바꾼다. (Groupby, Having - 그룹에 대한 조건)
- casting(숫자 -> 문자, 문자 -> 숫자)
- 평균, 합계 etc Having
- return 뒤에 값이 있는게 스칼라
- 람다 : 0 차원 리스트 : 1차원(라인, 선, 백터 .. etc)
-  ![[Pasted image 20251107101804.png]]
- 저장소 : 리소스 그 밖에는 소스(성능과 관련된거, 연산, 없으면 안돌아가는거)
- ![[Pasted image 20251107105429.png]]
- `소스, 스트림처리해서 코드가 많아야 좋음, 리소스는 줄여야함 가급적 해빙 쓰지마라
- `그룹바이 쓸때 미리 연산해서 테이블에 넣는거 추천 -> 리소스 줄이기 위해서(count도 같음)`
-  ![[Pasted image 20251107110301.png]]
- `해빙들어가는거 셀렉트에서 연산 미리 작업하고 감.`
- 리팩토링(Refactoring)이란 무엇인가?
	마틴 파울러가 정의한 **리팩토링**은 다음과 같습니다.
- "겉으로 보이는 **동작은 바꾸지 않고** 코드를 이해하기 쉽고, 수정하기 쉽도록 **내부 구조를 개선**하는 작업."
- `=하나의 메소드로 처리하지말고 쪼개라잇!`
-  ![[Pasted image 20251107110757.png]]
- 네트워크는 싹 오픈이어서 보안이 중요함.
- 커넥션은 지알아서 되니까 보안 필요 X.
- 리액트, 리액션이 발생함
- ![[Pasted image 20251107120626.png]]
- 파르메니데스 -> 원전(그림) -> Atom -> 상수 불변하는것은 구조 안에 있는 원자다.(상태)
- 헤라클레이토스  -> 스트림
- 리액트는 함수형임
- 컨트롤러 oop 아니게 만들거임
- ![[Pasted image 20251107121706.png]]
- 중립지역 : oop(서비스)->fp로 넘어갈때 컨트롤러로 뭔가함
- 프론트가 백으로 들어오기 전에 막음
- jpa, 
- React = 반응한다 = 화면이 나를 보고 인사한다 ㅎㅇㅎㅇ
- 라이브러리 <-> 프레임워크
- The library for web and native user interfaces
- 라이브러리, 스프링프레임워크 차이 - 질문 개많이함
- Spring Data JPA(구현체)
- hibernate
- ![[Pasted image 20251107124832.png]]
- 교차 엔티티 , 정규화 3공식
- 모든건 저렇게 3위 일체인데, 작게 보면 스프링 , 스프링Data(스프링과 데이터를 연결하는 놈)- 중재자(mediator), DB
- 리액트, nest js, 
- 리액트 스웨거 
- ![[Pasted image 20251107130451.png]]
- 서비스 , 임플(길), 레포지토리 임플 밟고 다니는데 서비스에서 왔다고 함.
- 스웨거 - 컨트롤러 자체가 길이고 impl임 레포지토리도 인터페이스가 있어야함. 진짜 구현된건 : 팩토리(뭔가 만들다) 메소드 
- 3개로 할때 맨앞에가 abstract  -> 중재자의 앞은 추상  => 추상팩토리
- 리액트(abstract) 넥스트(중재자) 스웨거
- 스프링 프레임워크 스프링부트(중재자) 컨트롤러
- 
- Next.js - The React (Framework) - 구현체 for the Web ,  리액트(The `library` for web and native user interfaces) 라이브러리 , 자바처럼 뭔가 넣겠네?
- 스프링 프레임 워크 : 구현체
- 스프링 부트 : 서비스, 스프링 프레임워크 : impl
- 스프링부트 : <u>스프링 프레임워크 + 자동 설정 + 내장서버 + 간편 실행 구조</u> , 
- 프레임워크: 껍데기, 실제 엔진 스프링부트 -> 둘다 있어야함.  라이브러리(dependency, DI 껍데기에 집어 넣는 행위) 