- 규칙기반으로 틀을 짜고 정책 기반을 집어 넣는다.
- 생성이 필요없는 데이터 : 규칙
- 생성이 필요한 데이터 : 정책
- 코일렉트리카 규칙기반이냐 정책기반이냐(글을 읽는다 리더기) 판단을 하고 규칙이면 안넘기고 정책이면 엑사온에 넘긴다. 
- 규칙기반이면 엑사온 아닌 서비스로 넘긴다.
-  
- models 여기에 있는 훈련된 모델 파일을 artifacts로 이동시키는데, 종류별로 폴더를 만들어서 너가 분류해줘.
- email_model.py에 여기에 mail_router.py 사용하는 email request를 BaseModel로 작성해줘
-  
-  엑사원 중심이고 각각 어뎁터를 분화시켜서 A를 잘하는 놈은 A어뎁터를 쓰고 B를 잘하게 하는 놈은 B어뎁터를 쓰고, 이런식으로 작업을 할 예정이니까 중앙에 있는 엑사원에서 로드된 리소스를 가져와서 사용을 할거야. 하나의 노드가 게이트 웨이 역할을 하고 그 노드 안에 있는 코일렉트리카는 리더기의 역할만해. 모든 판별 판독은 엑사원에서 할거야. 코일렉트리카 규칙기반이냐 정책기반이냐(글을 읽는다 리더기) 판단을 하고 규칙이면 안넘기고 정책이면 엑사온에 넘긴다. 엑사온은 툴로 감싸는 형태야
artifact 루트로 common = shared gateway- 는 스프링이 됨, training도 루트로
