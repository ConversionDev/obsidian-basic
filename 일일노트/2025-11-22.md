- 깃 브랜치
- 메인
- 디벨롭(머지) 
- 피쳐(각 개발자)
- 핫픽스(오류) -> 트러블 슈터
- 릴리즈(v) 이전 버전 저장(메인에서 완성품으로 나왔던 것들, 배포되었던 것들)
-  
- 프 : 버셀, 백 : 아마존(AWS) 배포
- 스파크, ai서버 구축 할 예정
- 
- 손익 계산, 재무재표(ESG 배우기)  - 유료 전환시 얼마 벌건가, 서버 비용 등등(나중에 두달 ESG할때 진도 안나갈때 할일)
-  
- 현재 우리가 구축한 시스템이 인프라를 구축하고 있습니다. 
- 현재 시스템은 깡통(데이터가 없다)입니다. 
-  
- ![[Pasted image 20251124095550.png]]
-  컴포(화면에 안보이는 부분 - 회로, 내용물), 컨테(화면에 보이는 부분 - 내용물 둘러싼 패키지, 깡통)
- 목업(가짜 - 휴대폰 매장 눌러도 반응없는 폰, 이미지만 있는거 작동안하는거 : 컨테이너) 
- 인풋 박스안 화면에 입력되는 데이터(state - 컨테이너) 
- 상태없음 : 널(앞으로 쓸거에요), undefined(버려) = `무상태` (stateless) <-> `유상태`(stateful)
- 
- 무상태 프로그래밍 :  깡통을 만든다. -> 들어온 데이터 값이 없어야한다 -> 메모리에 값을 두지 않는다. 
- Next.js , 리액트, 주스탠드, 리액트 쿼리, 스프링 게이트웨이, 스프링 부트 MSA, 레디스, 포스트그리에서 무상태 프로그래밍을 구현하는 과정을 초보자도 알기 쉽게 알려줘 - x에 있음
-   
- 기본적으로 로그인이 되어 있는 상태를 무상태로 해버리면
- 무상태 장점 : 서버 확장성(Scalability)
- 무상태 단점 : 로그인을 유지해야할때 못함, 쿠팡에서 장바구니에 묶음 배송이 안됨, 이미 이전에 데이터가 날아가버리니까 (레디스에 로그인한 데이터를 넣어야함)
- 원래는 무상태 가지고 하면 안되는데, 레디스 가지고 하면 무상태로 처리해야한다.
  
- stream/flux/flow : 상태가 흘러감.
- 현재 프로젝트에서 무상태 프로그래밍을 하는데, 쿠키, 캐시가 이것을 방해하고 있어. 현재 레디스 까지는 설치했는데 스프링 게이트웨이 설정을 어떻게 추가할까 -x에 있음
- 
- 스트리밍/실시간 기능이 필요
- 
- 스트림 : 동기 flux, flux/flow 비동기 
- 
- 모바일에서 비동기(flow)
- 
- 넷플릭스 유레카(유레카, 컨피크, 디스커버리) 
- 동기를 비동기로 바꿈 -> 게이트웨이를 수정하기 위해서 -> 빅데이터 처리를 효율적으로 하기 위해서 -> 파이썬을 깔기 위해서
- 
- 비동기(플럭스 - 토글)
- 게이트웨이, 로그인
-  
- 심플한 화면만 제공하는 cli npx create-next-app@latest my-login-app
- ![[Pasted image 20251124141701.png]]
- 
- 카카오 로그인
- ![[Pasted image 20251124154740.png]]
