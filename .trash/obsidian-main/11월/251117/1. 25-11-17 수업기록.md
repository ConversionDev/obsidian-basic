- 어플리케이션 컨텍스트(기존에 배운거)와 퍼시스턴스 컨텍스트의 차이
	- 컨텍스트가 맥락 뿐만 아니라 그 외로 어떤 의미가 있는지 알아야 함 = 환경

- 라이브러리(리엑트)와 디펜전시(스프링) 차이점
	![[Pasted image 20251117095107.png]]
	- 모든 언어에서 이런 것들을 "package 패키지"라고 함
	- 폴더가 패키지, 오른쪽이 파일, 합치면 라이브러리
		![[Pasted image 20251117095654.png]]

- 루트의 빌드그레들과 싸커 안 빌드그레들 다르게 생김
- ?? **대문자** 있으면 **라이브러리**, 아니면 패키지, ==아스타 붙은건 라이브러리?,== 맨 윗줄 임포트 위에 있는 package  뒤 내용은 패키지
- 패키지들을 노테이션으로 모아둔 게 디펜전시
- 설치를 하는건 패키지, 사용하는 거는 라이브러리
- ==??상태에 따라 패키지인지 라이브러리인지 달라지는 것==
- 노테이션 밖에 있으면 라이브러리, 노테이션 안에 있으면 디펜전시
	- 임포트하는 건 디펜전시가 아니라 라이브러리인거지
	- **임포트 하는 건 라이브러리다**
- 빌드그레들 안에 있으면 디펜전시

- GPT) 마이븐에서 디펜던시를 이루는 그룹아이디와 아티팩트 아이디의 관계와 그래들에서 이것이 어떻게 부르는지 설명해줘
	- groupId: 그룹의 식별자 팩토리 해당하는 부분(예. org.projectlombok)
	- artifactId: 폴더링 (예. lombok)
	- 폴더링 안에 자바파일까지 + 그룹아이디 = 라이브러리
	- 그룹아이디 + 아티팩아이디는 패키지, 라이브러리가 되려면 끝에 " .대문자 " 와야해
	- GPT) 컨텍스트에서 네임스페이스는 뭘 뜻하고 어떤 역할을 해?
		- 네임스페이스(namespace)**란 **이름이 충돌하지 않도록 이름을 구분하는 범위**를
		- 컨텍스트에서 네임스페이스는 **특정 영역에서 이름, 리소스, 설정, 객체 등이 유효한 범위를 정의**합니다.

com/labjang/api/common
여기서 api 앞에가 도메인, api(서브도메인)가 그룹아이디, 뒤에가 아티펙아이디
그룹아이디+아티펙아이디= 네임스페이스로 생각해. 다만, 실무에서는 아티펙아이디를 네임스페이스로 부르는 경우가 많아. 아티펙아이디(폴더가)많아지면 개별 하나씩은 유니크해지니까.

- 컴포넌트
	- 리액트에서 컴포넌트=아토믹 
		- 아톰-텍스트-페이지
		- 아톰 1개 = 페이지
		- 템플릿까지 오는 게 레이아웃
		- 플레이어 폴더 안의 모든 자바 파일들을 컴포넌트로 묶어
	- 

- 환경: 컨텍스트의 집합
	- 컨텍스트: 무대, 
	- 컨테이너:  컨텍스트에 컴포넌트가 등록됨. 컴포넌트를 담아
	- 컴포넌트: 컨서레엔
		- 사커라는 컨테이너 안에 팀,플레이어,스케줄,스타디움이라는 컴포넌트들이 들어있어
		- 데이터이동은 컨텍스트끼리 이동하는 것임
		- 컴포넌트 + 도커= 컨테이너

- 트랜잭션: 서로 다른 컴포넌트들을 연결하는 것 = AOP
- ACTX 로 컴포넌드(컨서레엔)만들고 > AOP로 트렌젝션해서 컴포넌트를 연결 > 그것들을 담은게 컨테이너 > 컨테이너를 담은 게 컨텍스트
- 컨텍스트끼리도 트렌젝션을 해서 연결해.
- ==??? 프로세스가 , 스레드가 메소드??==

==테이블 띄워야 해==

사진에서 : 앞에가 그룹아이디, 뒤에가 아티펙아이디
소문자는 무조건 폴더

- UI 커서 수정하기
	- CURSOR) 도커파일과 도커컴포즈파일로 구성하여 @app 이 넥스트 프로젝트를 컨테이너화 하는 코드를 작성해서 실행해줘.

-----------------
### 오후, 화면연결하기
- 강사님 엑스 https://x.com/lab_zang/status/1990278189285777913
- 컨테이너 이름 = 서비스 이름
- 리엑트 컨텍스트랑 스프링 컨텍스트 연결하려면 docker network를 써야 함
	- API 커서 도커 컴포즈 야물에서 맨 밑에있는 networks 부분임
- 커서 켜 놓은거 다 끄고, ui-sever 폴더를 복사해서 spring 서버2 에 붙여넣고 frontend로 바꿈
- 스프링에서 커서 키고
	- CURSOR) @docker-compose.yaml (루트에 있는거) 여기에 @docker-compose.yml(UI=frontend에 있던거) 이거를 옮기고싶은데, 도커 네트워크가 spring-cloud로 통합되도록 해줘.
- docker ps 터미널에 쳐서 뜬 컨네이너 이름들을 다 긇어서 다음 프롬프트에 붙여넣기(서치컨트롤러 만들어짐)
	- CURSOR) @powershell (45-55) 이 컨테이너 중에서 ui-server과 erurka-server 를 연동시키고, soccer-service 로 엔드포인트를 작성하려고 해, 이 과정에 적합한 전략을 알려줘. 실행은 하지 않고. (*이 때 중요한 것은, 커서에서 Auto 말고 Sonet 4.5로 바꿔야 함 )
		![[Pasted image 20251117162236.png]]

- 다시 Auto로 바꾸고 다음 프롬프트 입력
	- CURSOR) 전략 1: API Gateway 사용 (강력 추천) 이 패턴으로 진행하는 것을 승인한다. 이대로 커서가 코딩해줘
			![[Pasted image 20251118102936.png]]
- 다음 프롬프트 치기
	- CURSOR) @page.tsx 여기에서 @page.tsx (27-32) 이거를 실행하면, @eureka @discovery 를 거쳐 @PlayerController.java 여기로 데이터가 이동하도록 클로드가 전략을 알려줘.  (나중에 서치컨트롤러로 바꿈)