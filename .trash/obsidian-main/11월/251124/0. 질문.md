- 언어에서 함수 종류가 순수(일반)와 람다로 나뉨, 11/4일 수업기록에 있는 `return new Messenger(200,"HomePage";` 부분도 람다는 같은 것인가요?
	- `return new Messenger(200,"HomePage";` --> 이것은 람다이긴 하나, 옛날 방식이다. 진짜 람다는 builder를 이용한 것이 람다이다. 
		- GPT) Member m = new Member(); m.setName(), m.setAge() 를 Member Class 위에 @Builder 로 정의했다면 위 코드는 빌더패턴으로 작성해줘.
			![[Pasted image 20251126081523.png]]



- 다음 사진에서 return 부분을 제외한 부분이 스콥인 이유?
	![[Pasted image 20251126082329.png]]
	- 코드에서 이진분류로 쪼개보기 
		![[Pasted image 20251126082847.png]]
		- 가장 큰 구조부터 나눠보면, import 로 시작하는 부분과 아닌것을 라이브러리와 라이브러리가 아닌 것으로 나눔
		- 2번째로는 임포트가 아닌 부분에서 export default function App(): React.JSX.Element 를 헤드, { } 인 부분을 바디라고 나눠.
		- ?? { } 안에서 <리턴> 앞 부분을 컴포넌트(기능이 있는 곳), 뒤 부분을 컨테이너(?? 화면이 있는 곳)로 나눌 수 있음
			- 구롬 스콥이 컴포넌트인가?


- 빌더는 올아그스를 대체한다


- 스웨거랑 docs 는 뭐가 다른가요?
	- 스웨거를 보여달라고 하는 명령어 (url 뒤에 붙이는)
	- 스웨거는 유레카로 백 화면을 보일 때 더 보기 좋게 해주는 것

- 자바에서 매핑은 자바와 디비(이기종) 연결하는 것, 리액트에서 바인딩은 리액트와 무언가를 연결하는 (이기종 무언가)는 것

- 힙 스택 큐
	- 큐는 선입 선출
	- 스택은 후입 선출
	- 큐와 스택은 선형구조
	- 힙은 비선형구조, 랜덤으로 메모리에 저장, key로 찾는다
		- 키엔 벨류 구조는 다 비선형이고
		- 큐와 스택은 인덱스로 찾으니까 다 선형 구조다

- 분자 원자 유기체 템플릿 페이지
	- 여기에서 원자는 분자의 자료구조, 유기체는 원자의 자료구조~~ 이런식으로 차원을 올리면서 동일하게 생각할 수 있음

- ?: 는 인터페이스 사용 / ?. 는 직접호출
	- 코드 보면, ?; 를 쓸 때는 앞에 interface 가 있음 
	- ?. 는 그냥 바로 사용함
		


- 400번대 에러는 프론트 에러를 뜻하고
- 500번대 에러는 백 에러를 뜻함