- 커서에서 프롬프트 칠 때 클로드랑 커서의 차이
	- 뇌 그림 없는 소넷 : 클로드 : 뇌 : reason, 기획, 전략
	- 나머지 지피티들 : 커서
	- = 종류
		- = 할당연살자
		- == 값만 같다  스트링 타입인 A 에 숫자를 넣고싶을 때
		- === 타입이랑 값이 값다.
		

- 넥스트 책 p.418 + GPT
	- getStaticProps: 옛날 라우터 방식이었음. deprecated(더 이상 사용하지 않는다)
	- 이제 App Router 방식(서버 컴포넌트 + fetch 캐싱)으로 전환하는 것을 강력 추천함
		- 컴포넌트는 넥스트도 있고 자바도 있는데, 서버 컴포넌트는 누구꺼인가? 
	- 라우터와 미들웨어(함수)는 중요
	- App Router: 
		- CURSOR) @frontend 여기에서 넥스트 초보자인 내가 이해해도록 앱 라우터 부분이 어디인지 클로드가 설명만 해줘
		- 위에꺼: 순수 함수 / 화살표 : 에로우펑션 
		- 람다(화살표있는거) / 어나니머스 / 훅: useStat
		- 함수 호출 = call
		- 미들웨어: 나도 콜 받아서 온 애지만, 내 다음 미들웨어도 호출할 수 있다(콜백)
		- 순수 함수: 콜백 못해서 하나만, 자기만 호출 . 람다: 콜백 가능
		- 화면은 순수함수(변하지 않는 게 좋아)
		- 이벤트는 버튼 하나에 여러개 뜨길 바라니까 람다로 처리
		- 콜백: 메모리를 잡아먹지 않는
		- 이름은 헤드, 그리고 바디
		-------
		- 타입에 두개: 함수는 상수(const) / 속성 값은 변수(var)
		- 동기=sync가 디폴드 / async=비동기
		- 1번: 동기, 바로 할당
			 ![[Pasted image 20251120115418.png]]
		- 3항 연산자로
			- === 으로 비교했을 때 트루면 : 앞에 내용을 리절트메세지에 넣고, 틀리면 ;  뒤에 내용을 넣어
		- undefined 와 null 차이
			- 참고: 자바는 언디파인드가 없음
			- def :  define 정의
			- 파이썬에서는 none
			- 주소가 자리임. 
				- 주소가 자체가 없는 것
				- 통은 있는데 주소도 없고
				- 비어있는 것
				- 주소가 있는 것



| 기호      | 뜻                                                                                                                                 |
| ------- | --------------------------------------------------------------------------------------------------------------------------------- |
| ? A : B | ? 앞에 조건문이 참이면 A를 반환하고 거짓이면 B를 반환한다                                                                                                |
| ?:      | optional로써 null이 있어도 되고 없어도 돼 = null만 허용(undefined는 허용 안 함)<br>예) message?:  >> 이 경우, message에 값이 있거나 null은 가능. undefined는 허용 안 함 |
| ?.      | null 이어도 상관 없다 (에러를 띄우지 않고 넘기겠다)                                                                                                  |
| A ?? B  | A에 값이 있으면 A 가 반환되고,  A가 null이나 undefined 인 경우 B를 반환                                                                               |


- 엘리먼트 리스트 자료구조
- 슬라이스 스토어
- 하나가 되면 방향성이 있어 / 컨서레엔은 방향성이 있어

함수인데 이름이 없는 거 어나니머스


화면을 먼저 짜고, 
슬라이스가 화면 별로 나뉜다 추후에는 결국 에이전트가 하나의 페이지에 하나씩 들어가니까
슬라이스가 에이전트로 보일 수 있음. 일단 기본 개념은 화면 1개당 슬라이스 1개임



