- 복습 한 줄: GW 를 플럭스로 바꿨다
- GPT) 웹 보안시스템에서 auth 에 대해 초보자가 이해하도록 쉽게 설명해줘
	- Auth = 인증(Authentication) + 인가(Authorization)
	- Auth는 로그인한 사용자가 누구인지 확인(인증)하고, 그 사용자가 무엇을 할 수 있는지 결정(인가)하는 웹 보안의 핵심 시스템이다.
	- 
- 나중에 .env보다 더 보안이 높은 걸로 바꿀거다
- 지금은 버셀에 프론트를 올렸는데, 나중에 aws에 올릴거야
- 프론트랑 백엔드를 분리해야해 무조건
	- 컨텍스트 분리되어서 오류 많이나고 힘들어도 보안때문에 분리하는 거야
- 백엔드에서 services = BE / gateway = GW
- 결국 FE <-> GW <-> BE
- GET vs POST
	- GET: 조회 / 바디 없음
	- POST: 생성·전달 / 바디 있음 / 보안에 민감한 데이터 전송

	- URI
	- CORS
		- async-await : 비동기를 뜻하는 것. 프론트는 무조건 async-await를 써야하는 것. 게이트웨이가 플럭스니까
		- 노테이션에 헤더(이름) 없으면 json이라고 불러야해. 이름 없는 애를 제이슨이라고 하니까
		- ==credentials은 나중에 쓰자. 쿠키 사용할 때 필요==
		- 헤더와 바디가 있으면 기능이 없는거라 객체인지 아닌지 모름. = html
			- 헤더에서 ‘이건 JSON이다’라고 알려줘야 자바가 바디 내용을 제대로 객체처럼 이해할 수 있다.
		- JSON = { }  대문자면 노테이션을 뜻해 / json 소문자면 타입을 정해준 것임
		- stringify = 문자열로 하게 하다 = 선형 = 제이슨은 비선형 = 비선형을 선형으로 바꿔라 라는 뜻 ( 프론트<->자바 사이 전송할 때는 선형이어야 선에 태울 수 있음)
		- 키가 기계어면 선형, 키가 자연어면 비선형
		- 프론트에서 fetch = 백에서 find 라고 함,
			- ??? 보낸다가 아니라 가져온다라고 하는 이유?
		- 싱크는 접속한다는 의미. 
		- 훅과 헨들러 차이
			- 훅은 스토어한테 주는 거
			- ??? 장바구니 같은 게 스토어에 저장되는거야. 디비에 저장하는 게 아니라. 결제하면 스토어에 있는 거 디비로 넘기는거지
			- 헨들러는 스토어와 연관되어있지 않고, 훅한테 줘야되는데 . 훅은 ????
			- 헨들러중에 어씽크 웨이트 있는 애는 없는 애는
			- ??? 헨들러하고 훅은 람다다. 순수 뭐시기
			- 화살표 있으면 람다 함수지
			- 순수 = 일반 VS 람다
			- POST 는 준다  만든다  =INSER/  FETCH 는 갖고온다
			- 겟은 바디가 없다.
			- requestbody = JSON

- 프론트도 백의 게이트웨이도 모두 플럭스라서 비선형구조(json)인데, 프론트와 자바를 왔다갔다 하려면 선형구조여야 해서 바꿔주는 작업이 필요해. JSON 이라는 비선형 형태를 stringify 로 선형화해서 보낼게, 근데 header에 json이라고 비선형 구조를 말해줌으로써 자바가 선형구조로 받아도 비선형구조라고 알고받으라는 것.




-----

- 백 수정) @KakaoController.java 여기에서 @RequestMapping("/api/auth/kakao") 여기 중 api 는 @ApiApplication.java 이 게이트웨이에서 /api를 판단하도록 수정해줘.
- api는 메인, 서비스들 안에 있는건 서브라우터라고 함
- 백 수정) @application.yaml 여기에 스프링 클라우드 게이트웨이 문법에 맞게, 마이크로 서비스 컨테이너를 로드밸런서로 등록해서 처리해줘. /auth 는 서브라우터로 처리해줘.
- 



- 자바에서 노테이션이 바디. 그 앞이 헤드