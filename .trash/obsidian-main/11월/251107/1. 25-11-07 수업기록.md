- GPT) sql에서 어그리게이션이 뭐야?
	- SQL에서 **어그리게이션**(aggregation, 집계) 은 여러 행(row)의 데이터를 요약·집계해서 하나의 결과를 만드는 연산을 말해요. = **벡터를 스칼라로 만든다는 것** : SQL에서 **GROUP BY + HAVING를 사용**.
		- 그룹바이는 셀렉트(리턴)에 있는 것, 헤빙은 라운드에 있는 것 
		- 단일결과값=스칼라, 여러행=벡터
			- *스칼라 <-> 벡터 : 어그리게이션
			- *참고: 숫자 <-> 문자 : 캐스팅
			- 람다는 0차원, 리스트는 1차원
	- 스칼라(공학)를 자바에서 람다이고 단수임
	- 벡터(공학)는 자바에서 스트림이고 복수임
		- 디티오는 스칼라지만 그 안에는 벡터임
		- 벡터는 리스트와 어레이 두가지로 나뉘는데, 이기종 타입끼리 묶이면 리스트, 동일끼리 묶이면 어레이. 자바는 둘 다 가능


- 리스트와 어레이리스트의 차이
	- List<?> ls = new ArrayList<?>( ) ;
	- 리스트: 인터페이스 (형태만 정의, 실제 구현 없음)
	- 어레이리스트: List의 대표 구현체 (실제 데이터 저장 가능)

- 리소스(화면)를 줄이고 소스(자바)를 늘려라
	- HAVING 사용 최소화. 최대한 스칼라로


- GPT) 마틴파울러와 리팩토링이란?
	- 소프트웨어 유지보수의 핵심임
	- 외부 동작(기능)은 그대로 두고, 내부 구조(코드)를 개선하는 것

------------
#### 화면

- 노드 다운 받기 강사님 엑스: https://x.com/lab_zang/status/1986622849612943461 
- https://nodejs.org/en/download 접속
- npm 오류 ![[Pasted image 20251107114518.png]]
- 터미널 관리자권한 실행으로 다시 켠 후 다음 명령 실행
	Set-ExecutionPolicy RemoteSigned
- 후 Y 입력


----
- GPT) 리액트란?
	- 웹사이트 화면을 만들기 쉽게 도와주는 도구
	- 기존의 HTML은 웹페이지 한 군데가 바뀌어도 전체 페이지를 다시 그려야 하는데 리액트는 화면을 조각으로 나눠서 관리하기 때문에 관리하기가 쉬워지는 것

	- REACT.DEV 홈페이지 접속
		- 파르메니데스의 철학이 적용함
			- 헤라클레이토스: 강물은 흐른다. 스트림
			- 파르메니데스: 상태가 변하지 않는다. 불변하는하는 것은 구조 안의 내용이다. 물은 흐르지만 물의 원자는 변하지 않는다. 


	- 리액트는 화면을 작은 조각(컴포넌트) 단위로 함수처럼 만들어서 편리하게 조립할 수 있도록 도와주는 UI 도구 세트다.
	- 리액트는 라이브러리다. (반대개념은 프레임워크)
		- 이니셜라이저 다운받을 때 골라서 다운받았던 룸북, 타임리프 등등을 라이브러리라고 함

- 넥스트 다운로드 --> 2번 수업기록 참고

----------
- 화면 만들고, 지피티처럼 화면 수정해달라고 해
- f12 누르고 아래 사진 아이콘 클릭
	![[Pasted image 20251107175211.png]]

- 여기 커서 갔다 대고 스크립트 창에서 해당되는 부분 클래스 옆에 글 부분 복사. 커서 돌아가서 page.tsx 탭에서 찾기에 복붙해서 해당하는 인풋이랑 버튼 내용 찾기 ![[Pasted image 20251107175236.png]]![[Pasted image 20251107175410.png]]

- @page.tsx @page.tsx (33-36) @page.tsx (38-42) 이 버튼을 클릭하면 input박스에 있는 텍스트를 알러트(alert) 로 띄어줘