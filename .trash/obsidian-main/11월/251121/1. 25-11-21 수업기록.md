- 미들웨어
	- 미들웨어는 함수다
	- 미들웨어는 소프트웨어와 하드웨어 사이를 연결해주는 교차엔티티 같은 것
	- 따라서 미들웨어는 양 쪽 부모의 PK를 FK로 가지므로 2개의 FK를 가지게 됨
	- 미들웨어는 리액트의 무언가와 자바의 무언가를 가지는 것이라고 생각하면 편함
	- 한줄짜리였는데 두줄짜리가 될거다
		- 자바는 한줄이다. 두줄보다 레벨이 낮아
	- 미들웨어는 콜백함수다 = 미들웨어는 연쇄적으로 다음 미들웨어를 호출할 수 있따
	- 라우트 단위의 미들웨어 = 미들웨어가 묶음으로 있을 것이다
	- NEXT 15 에서 앱라우팅으로 바뀜
	- 컴포넌트는 훅, 컨테이너는 헨들러
		- 헨들러는 콜백임
	- 훅이 헨들러 부르고 헨들러가 미들웨어를 부름
	- 메서드체인은 디자인패턴 중 책임연쇄에 해당함. chain of responsibility > chain > 콜백함수다
		- 따라서 헨들러, 메들웨어를 모두 메서드체인이라고 볼 수 있음
		- 훅은 맨 처음에 있는 갈고리임. 콜백 못 함
		- 책임은 기능이다. (속성이 아니라)
	- 미들웨어를 이용하면 각 라우트에 대한 공통된 로직을 분리할 수 있다(AOP 개념)
	- SOLID = 객체지향 5대 원칙
		- S : 단일책임원칙 
			- 하나의 클래스 또는 하나의 책임(메소드)만 가져야 한다
			- 지금은 하나의 클래스 안에 여러개의 메소드가 들어가있지만, 앞으로는 하나씩만 들어가도록 분리해서 체인으로 연결해라
	- 미들웨어는 HTTP에서 규정한 헤더를 준다. 우리는 BODY만 주면 된다. 헤더는 이름이다.
		- 바디는 노테이션으로 구성된 것 = JSON만 만들어주면 돼 = 이름이 없어 = 주소가 없다
	- 미들웨어를 쓰면 자바 없이도 DB를 갈 수 있다 (CEO인사말 같은 연산이 필요 없는 것들)
	- ??? 미들웨어는 에러를 잡을 수 있다. 500이 어디서 와?
		- 비동기 에러는 잡을 수 없는데 방법은 있다
	- ==레디스==는 경량 데이터를 연결하고, 포스트그리는?? 중요한 데이터를 연결해
		- 도커 컴포즈 야물에 레디스를 추가해줘 접속기록, 에러같은거
			- CURSOR) @docker-compose.yaml 여기에 레디스를 추가해줘
			- 팀원끼리 레디스에 둘 거 정해
		- 페이징: 전체를 다 갖고오지 않고 페이지 일부를 갖고 오는 것
			![[Pasted image 20251121110111.png]]
	- GPT) 리액트에서 FC란 무엇인가?
		- React에서 컴포넌트를 **함수 기반**으로 작성할 때 쓰는 타입(타입스크립트) 또는 패턴을 말해
	- FC : 함수형 컴포넌트
	- ??? 헨들러가 컨트롤러가 된다


- 미들웨어와 api 차이
	- 미들웨어가 보안이 더 나아
	- api - key 값??


- 권한설정
	- 자가진단으로나오는 실시간 등급 결과는 자사결과만 스스로 볼 수 있게(관리자 포함)
	- 최종결과 보고서


버셀에서 가비아 도메인 연결하는 데 가비아 권한을 버셀에 넘기는 법

피그마 생성 후 앱라우팅 구조로 바꿔달라고 해야함
