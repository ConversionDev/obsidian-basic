- 복습
	- ==어둠속, 비밀로 디비랑 연결하는거 **커넥트**: 자바랑 디비 연결, 사실 다 은닉화되어있음
	- ==공개적 연결은 **네트워크**: 리액트랑 자바 연결==

- 자바에서 자료구조는 리스트(목차)와 맵(키워드)
- 리액트에서 프롬프트 (이전에는 커멘드) cli가 커멘드
- 질문: 리액트에서는 런 버튼이(GUI방식) 없는데 어떻게 로컬호스트로 어떻게 연결?
	- 리액트에서는 CLI 방식으로 해야 함
	- 클로드) 이제부터는 cli로 직접 npm에게 명령(커멘드)을 하고싶어. 프로젝트를 실행하는 명령어를 알려줘.
		- npm run dev

- 스택 - 힙 / 큐 
	- 프롬프트 창에서 사용하는 방식은 큐 방식인거지
	![[Pasted image 20251110100254.png]]
	- 힙은 로딩 속도가 빨라. 가비지 컬렉터가 처리함
	- 자바 메모리 처리 방식은 힙. 스택은 리스트에서 처리하는 방식
	- 큐 방식이면 커멘드가 쌓여. 그게 cpu의 일정 부분을 차지하는 데 그걸 캐시메모리라고 함


- 타입과 객체는 다름. (공통점은 둘다 속성과 기능의 집합임) 차이는 **값의 유무**임. 
	- 타입은 스키마, 객체는 튜플 혹은 인스턴스 혹은 스키마
	![[Pasted image 20251110095126.png]]

- 커멘트 패턴
	- 요청(명령)을 객체로 만들어서 실행, 취소, 기록 등을 유연하게 다루는 패턴
	- 만들어서 객체로 감싸버리되 다른 사람들이 사용할 수 있게 해줌(재사용성)


- NODE  와 NPM
	- NODE: (타입)실행 환경. 자바스크립트를 서버나 로컬에서 실행할 수 있게 해주는 프로그램
	- NPM: (객체)패키지 관리자. 노드 위에서 동작하는 도구
		- 환경이 타입. 객체가 서있는 맨 땅
		- 그 위에 객체가 서는 것
		- 실행할 때 NPM 어쩌구 저쩌구 CLI에서 docker처럼 친다는 것
	- 컴퓨터는 기본적으로 npm 이 디폴트임. 이걸 pnpm으로 바꿔야 해.
		- 

- 식당에가서 바로 순대국을 보는 게 아니라 메뉴판을 봄. 가상인. 그 가상들을 다 인터페이스라고 함. 접점은 다 인터페이스


- GPT) 내가 예전에 jquery 쓸 때는 npm을 사용했는데, 이제 리액트로 기능이 향상된 시점에서 yarm과 pnpm 중에 어느것을 추천해?
	- pnpm이 가장 빨라서 우리가 사용해야 하는 것. 
	- ==npm → pnpm 전환 전체 CLI 정리하는 법==
		- pnpm 설치: npm install -g pnpm
		- 기존 npm 잔여 파일 제거: Remove-Item -Recurse -Force node_modules, package-lock.json
		- pnpm으로 의존성 재설치: pnpm install
		- pnpm이 정상 설치되었는지 확인: pnpm -v
		- 현재 프로젝트가 pnpm 기반인지 확인: pnpm list
		- 실행 확인: pnpm dev

- GPT) 객체가 있다면, 객체 모델이란?
	- 프로그램이 처리해야할 대상을 추상화하여 데이터(속성)와 행동(메서드)를 하나로 묶어 구조화 한 것
	- 자바에서는 도메인 중에 특히 DTO에 해당 함

- 포조 POJO
	- (Plain Old Java Object)는 노테이션만 갖고있고 어노테이션은 없는 것.
	- 특정 환경이나 프레임워크에 묶이지 않고 필요에따라 재사용될 수 있도록 설계된 객체
	- 어노테이션 붙은 후에는 "BEAN 객체"라고 함

![[Pasted image 20251122153736.png]]


- 리액트의 발전역사
	- 리액트는 돔을 사용해서 객체를 만들거다
	- 돔은 객체 모델임
	- 객체냐 아니냐 물으면, 타입과 객체로 나눠서 생각을 해야 해. 
	- DocTYPE: 은 타입이니까 객체가 있어야 해. 그 객체를 뭐라해? 
	- 자바에서 타입과 객체면, 리액트에서는 HTML(=DocTYPE)과 DOM 임.

	- 돔: 타입이 HTML인 객체
	- 구글 검색창 : HTML
	- 입력하고 나서도 HTML 인데, 값이 들어간거 = 돔이다
	- 돔은 HTML이 맞아

	- 돔의 단점은 성능저하임
	- 리액트는 돔을 실제 돔과 가상의 돔으로 나눔
	- 가상의 돔이 빠름
	- 상태가 바뀌면 react가 알아서 화면을 갱신한다(==상태페턴== 중 행동패턴. 17번)
		- GPT) 리액트에서 아토믹 패턴에 대해서 알려줘
			- 아톰 = 원자 : 자바의 인스턴스, 리액트의 돔
			- 컴포넌트 = 분자 MOLECULES 
			- organism = 유기체
			- template
			- page
			![[Pasted image 20251110121344.png]]

- 유기체부터 볼 수 있어

![[Pasted image 20251110123635.png]]
- 레이아웃 = 템플릿
- 페이지 = 컨트롤러
	![[Pasted image 20251110123505.png]]
- 그럼 원자 분자 유기체가 없네? 나중에 분리시킬거야
- 속성 기능 객체 자료구조 ? >> 얘네 다 객체. 모든 것은 객체야
- 그래서 상태가 중요해지는 거야.

- 3번에 { } 있네 = 노테이션 있네 = 객체다
- { } 안에 uesState(동사) 가 있네 =  
![[Pasted image 20251110125454.png]]


?? 자바에서 람다, 스칼라, 컴포넌트
컴포넌트는(객체) 속성과 기능을 // 상태와 hook라고 함
바깥쪽에 있는 기능이 함수, 안에 있는 기능이 hook
자바에서 리턴, 리액트에서 렌더링이라고 함

상태패턴을 쓸거야!!!
함수형 쓸거야!!!

- < > 기준
	- 나가면 컴포넌트
	- 들어오면 엘리먼트

클래스를 인스턴스 해줘서 뉴나 어노테이션 객체
컴포넌트를 

- 이 중에 무엇이 객체일까?
	- < Button > : 객체
		- label: 속성 / onClick : 기능 
		- 1번이 컴포넌트, 원자(돔을 물고있는 애가 원자) 네임드
		- 2번이 엘리먼트, 태그, 돔(html), 실제돔
		- 4번(원자)를 물고있는 애는 최소 원자 이상이라는 거지, 가상돔 
		- 펑션으로 시작해서 이름을 쓰면 컴포넌트
	![[Pasted image 20251110131411.png]]


- 컴포넌트: 형 이상 : 아톰처럼 생겼어
- 타입: 형 이하 / class 가 형 이상
- 함수 안의 함수 : 훅 (함수를 객체로 정의했기 때문에)

- 리액트에서 컴포넌트 작성하기 = 자바에서 class 만들기
- 컴포넌트는 다섯가지 종류: 원자 분자 유기체 템플릿 페이지


- 3번라인에 react에 임포트를 모아뒀고, 그 중에 useState를 가져올거야.
- 3번에 useState 가 리액트야 . 인풋박스를 인지하겠다
	- 사용자가 입력한 값으로 상태를 변경해줘
	- 뭔가가 쓰여지고 있다
- 6번에 useState 비어있는 상태를 체크한다. 무언가가 쓰여지면 반응한다. 원래는 무반응
	![[Pasted image 20251110161649.png]]

- 훅과 호이스트
	- 호이스트가 전역 / 자바스크립트
	- 훅이 로컬 / 리액트 

- use를 안쓴 8번은 이벤트라 함. 개발자가 만든거


console.log.message
파사드



- GPT) 함수에서 이름이 있는 지 없는지에 따라 구분해서 알려줘
	- 함수를 이름이 있는가 없는가로 구분하면 명명된 함수(named)와 어나니머스 함수로 나뉨. 이름이 있어야 객체가 되는건데 이름이 없는것도 객체가 될 수 있다는 거지.
	- 개발자에게 좋은 건 명명된 함수
	- 그렇다는 건 컴퓨터한테는 안 좋다는 것
	- 어나니머스 함수는 일회성이고 즉시사용으로 컴퓨한테는 좋음
		- This : 이름이 없으니까 부를 수 없어서 사용하는 방법
		- 일반적으로 람다 형태로 표현됨(또는 arrow function)
		- 이름이 없기 때문에 디버깅이나 재사용 어려워 개발자에겐 어려움
		- 어나니머스보다 어로우펑션이라고 더 많이 써

- spa
	- **SPA** (e-북) (Single Page Application) 은 말 그대로 “페이지가 하나(Single Page)인 웹 애플리케이션”. 개발자 1명만 필요
	- **MPA** (책) (Multi Page Application): 개발자 여러 명 필요
	- 보일 때만 객체이다
	- 


파이썬: 람다
자바스크립트, 리액트: arrow 함수: => 
자바: -> 



- 타입스크립트가 빌드되면 html이 돼.

-------------
- UI 커서에서 폴더링 앞에 아이콘 있는 거
	- TSX: TS의 확장판
		- TS tsx = new 
	- TS: 타입
	- { }: 노테이션

- 인터페이스는 기능에 대한 타입
- 클랙스는 속성에 대한 타입




검색창에 있는 게 변수
검색했을 때 나오는 게 상수

구글 검색창 : HTML
입력하고 나서도 HTML 인데, 값이 들어간거 = 돔이다
돔은 HTML이 맞아



------------------
### 스크립트 실습

- ui 클로드) @page.tsx @page.tsx (43-70) 이거를 console.log(message) 이거로 바꿔줘

- api 클로드) @soccer 리액트에서 axios 로 검색 쿼리로 findByKeyword 로 검색어를 주는데, player, team, stadium, schedule 중 어느 질문이 들어올 지 모르겠어.나는 이것을 파사드 페턴으로 해결하려고 하는데 더 좋은 페턴이 있으면 알려주고, 아니면 파사드 패턴으로 넘어온 검색어를 sysout로 출력까지만 하는 코드를 작성해줘.

- API 창) 입력창에 ss를 적은 게 지금 ui 콘솔에 보이잖아. 이게 http://localhost:8080/api/soccer/search 여기 주소로 axios를 이용해서 전송할거야. 백엔드에서는 뭘 수정해야할까? >> 아래 처럼 알려주면, 이걸로 수정해달라고 함
	![[Pasted image 20251111101005.png]]

- UI 창) 입력창에 ss를 적은 게 지금 ui 콘솔에 보이잖아. 이게 http://localhost:8080/api/soccer/search 여기 주소로 axios를 이용해서 전송할거야. 프론트에서는 뭘 수정해야할까?
	- 매핑 방법은 get 고 keyword 로 주도록 해줘 >> API에서 사용한 메핑이랑 방식이라서 수정해줬음

