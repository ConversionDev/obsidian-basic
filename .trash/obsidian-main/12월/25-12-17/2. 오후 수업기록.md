- 하이브리드 서치 hybrid search
- Qdrant = jdk???


- 아래처럼 폴더 변경및 추가
	![[Pasted image 20251217145821.png]]
	- maon.py 기준으로 시블링으로 폴더 4개 만들어

어제는 OPENAI_API 와 프론트를 연결하여 대화하는 걸 했었는데, 그 연결을 끊고 오늘은 @midm 여기에 있



from transformers import AutoModelForCausalLM, AutoTokenizer model_id = "K-intelligence/Midm-2.0-Mini-Instruct" model = AutoModelForCausalLM.from_pretrained( model_id, torch_dtype="auto", device_map="auto", trust_remote_code=True # Mi:dm 필수 ) tokenizer = AutoTokenizer.from_pretrained(model_id) --> 이것을 가지고 @app/model/midm 이 모델을 읽어서 로드하는 코드를 적당한 곳에 파일을 생성해서 코딩해줘.



python -m backend.app.main