

- ==포스트맨에서 뜨고 터미널에 안보일 때, 터미널 새로 열어서 아래 명령어 입력하면 보임==
docker-compose logs -f ml-service


- 복습
	- 데이터 프레임을 다룰 때 쓰는 클래스 이름이 pandas 판다스임
	- 웨어하우스에서 셋을 먼저 뽑고 필요한걸 프레임으로 뽑아
	- 데이터프레임은 데이터 셋 안에 있어야 하는 것

- 레파지토리는 디비 연결용이라 없는거고 엔티티 대신 모델 서비스는 그대로 컨트롤러는 라우터  
- 데이터셋은 읽어들이는 것  
- 서비스는 메소드의 집합? 지난주 꺼 찾아보기  
- DS -> DF 과정을 로딩이라고 함

- 예시) 리뷰 데이터 셋을 만들 때
	- 평점이 지도, 글만 작성한 것이 비지도

##### method 탭에서 실습 : 지도학습 전처리
- CSV 가 DS임. 이걸 DF 로 바꿔야해
	- 아래 작업이 모두 끝나면 메서드 안에있는 것들은 모두 DF 가 되는 것임
		- model, train, label, features = DF (check_null 부분은 -> pd.DataFrame: 를 안 쓰긴 했지만 이미 DF인 것)
	- CURSOR) @train.csv 을 DF 로 전환하려고 해. @titanic_method.py 여기의 @titanic_method.py (9-11) 이 코드를 여기에 pd.read_csv 를 리턴하는 메소드로 작성해줘
		- pd가 csv를 읽는 메소드를 작성해줘 도 가능
		- df는 곧 객체다 이걸 디스크에 저장하면 모델. df 는 메모리에, ds는 디스크에 있음
		- 아래 사진 두개는 항상 짝궁임. 지도학습 하는. 
			![[Pasted image 20251208095625.png]]

- train은 라벨값을 제외한 것을 의미. (이 때 훈련이라는 한글을 쓰지마. 북한말 쓰는 것 같음)
	- train = DF - label
	- label = DF - train
- DS 는 값을 바꿀 수 없어
- [ ['Survived'] ] --> 이렇게 대괄호 두개로 쓰는 것은 스키마 중에서 괄호 안에 있는 것을 라벨로 뽑으라는 뜻임
 
- 지도학습 준비 (이것만 하는 사람을  데이터 라벨러 라고 함)
	- DS -> DF -> 라벨과 프레임을 분리

- DF 중 model에서 ==라벨을 제거한 것== = ==train==
	- CURSOR) @titanic_method.py (14-16) (#Survived 값을 제거한 데이터프레임 작성 부분) 여기에 @titanic_method.py (10-12) 이 DF 에서 Survived 값을 제거하는 값을 리턴하는 코드를 작성해줘

- DF 중 model 에서 ==라벨만 분리시킨 것==  = ==label==
	- CURSOR) @titanic_method.py (18-20) 여기에 @titanic_method.py (10-12) 이 DF 에 Survived 을 라벨만 분리시킨 DF 를 만들어줘

- 피처 삭제 및 널 체크 코드 짜기기
	- CURSOR) @titanic_method.py (18-20) 이 메소드 밑에 피쳐를 삭제하는 메소드와 널을 체크하는 메소드를 이름만 너가 생성하여 내부는 pass 로 만들어줘
	- **피처(feature)”는 머신러닝 모델이 예측을 하기 위해 참고하는 입력 변수(variable)들(설명 변수)** 을 뜻해. 쉽게 말하면,  **모델이 어떤 판단을 내릴 때 근거로 삼는 정보 조각들**이야. 피처는 편집 가능한 상태를 말해
		- 컬럼을 삭제하는 것은 말이 안되는 것임. DS는 값을 바꿀 수 없는 존재니까.
		- DS 에서는 컬럼, DF일 때는 피쳐 (공통점은 둘 다 variable)
		- 피쳐라고 부르는 순산 DF 이거나 DF로 갈거구나. 그리고 편집 가능한 상태구나
	- **그라운드 트루스(ground truth)** 는 **“정답(label)”**, **“실제 결과”**, **“진짜 값”** 을 뜻하는 말이야.
	- GPT) 아래 사진에서 아스타가 갖는 의미는? 
		- 튜플은 고정, 리스트는 변할 수 있기 때문에 차이가 있음
		- 
				![[Pasted image 20251208104948.png]]
		- 관련해서 커서 명령: CURSOR) @titanic_method.py (22-24) 트레인에서 피처를 삭제하는 코드를 작성해줘
		- CURSOR) @titanic_method.py (27-30)DF에서 널의 갯수를 리넡하는 메소드를 코딩해줘

- 메서드를 다 수정한 뒤면, 자바랑 비교했을 때 메서드가 서비스, 서비스가 서비스 임플과 같아지는 것
##### service 파일로 이동
- 아래 사진처럼 코딩
	- method 를 호출하는 작업으로 titanic DS 를 보는 것
		![[Pasted image 20251208111244.png]]
- a는 처음 나오는거, the는 불렀던 거 
	- 메서드 부분이 아래 사진처럼 되어있으면 CSV 파일이 10000개 들어오면 저 메서드도 10000개 만들어야 하는 문제가 생김. 따라서 두번째 사진처럼 fname 으로 해주면 됨
	- 그리소 다시 train_service 로 가서 세번째 사진 부분 처럼 넣어주면, 10000개 중 train.csv 파일과 연결되는 것임. 파란색 줄 부분 추가해주면 test.csv 파일과 연결되는 것
			![[Pasted image 20251208111824.png]]![[Pasted image 20251208112049.png]]![[Pasted image 20251208114031.png]]
		- 동그라미 친 부분 . 대신 _로 바꿈 
			![[Pasted image 20251208114242.png]]



- 서비스 부분 지웠던걸 새로 new model 하면 지운게 다시 생성되는 거니까 아래 사진처럼 바꿈
	- ![[Pasted image 20251208115438.png]]


- 파이썬 **리스트 컴프리헨션(List Comprehension)** 은 **한 줄**로 “리스트를 빠르게 만들기 위한 문법”이라고 생각하면 돼. 코드를 더 간결하고 읽기 좋게 만들어서 파이썬에서 매우 자주 사용돼.
	- CURSOR) @titanic_method.py (22-26) 이것을 리스트 컴프리헨션으로 변경해줘.


- ic 보다 logger 가 더 잘 보이는 것 같음



