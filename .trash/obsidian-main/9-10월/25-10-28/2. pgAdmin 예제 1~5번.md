select count(*) from player; --100 --여기서 주석은 마이너스 두개로 하면 돼

select * from stadium where tel = '273-1763' limit 1 ;
select * from stadium limit 5 ;

select * from stadium;
select * from schedule;
select * from team;
select * from player;

--where 빼면 다 하라는 소리. = * = 모든 칼럼



-- 001. 전체 축구팀 목록을 팀이름 오름차순으로 출력하시오
SELECT *
	FROM team
	ORDER BY team_name COLLATE "ucs_basic" ASC;

-- 002. 플레이어의 포지션 종류를 나열하시오. 단 중복은 제거하고, 포지션이 없으면 빈공간으로 두시오
select distinct 
	coalesce(position, '') as position
	from player
	order by position;
	

-- 003. 플레이어의 포지션 종류를 나열하시오. 단 중복은 제거하고, 포지션이 없으면 '신입' 으로 기재하시오
select distinct 
	coalesce(position, '신입') as position
	from player
	order by position;


-- 004. 수원팀에서 골키퍼(GK)의 이름을 모두 출력하시오. 단 수원팀 ID는 K02 입니다.
select player_name from player
	where position = 'GK'
	AND team_id ='K02'


-- 005. 수원팀에서 성이 고씨이고 키가 170 이상인 선수를 출력하시오. 단 수원팀 ID는 K02 입니다.
select * from player 
	where player_name like '고%'
	and height >= 170
	and team_id = 'K02'; 


----------------------
수업 후





-- 001. 전체 축구팀 목록을 팀이름 오름차순으로 출력하시오
--내 답안
SELECT *
	FROM team
	ORDER BY team_name COLLATE "ucs_basic" ASC;  --usc~ 랑 같은데 C 인데, 둘 중에 usc가 더 빨라

--강사님 답안
select t.team_name as "팀명"  --여기서 더블쿼터 쓰는 이유는, 결과값 컬럼을 새로 지정하는건 사람이 보는거니까 as"팀명
from team t 
oder by team_name asc;



-- 002. 플레이어의 포지션 종류를 나열하시오. 단 중복은 제거하고, 포지션이 없으면 빈공간으로 두시오
select distinct 
	coalesce(position, '') position  --as 만 지워
from player
order by position;



-- 003. 플레이어의 포지션 종류를 나열하시오. 단 중복은 제거하고, 포지션이 없으면 '신입' 으로 기재하시오
select distinct 
	coalesce(position, '신입') position
	from player
	order by position;


-- 004. 수원팀에서 골키퍼(GK)의 이름을 모두 출력하시오. 단 수원팀 ID는 K02 입니다.
select player_name
	from player
	where position = 'GK'
	AND team_id ='K02'

select player_name
from player t                       --여기서 왜 p / 다음줄에 p.
where t.team_id = 'K02'
and position = 'GK'; 


-- 005. 수원팀에서 성이 고씨이고 키가 170 이상인 선수를 출력하시오. 단 수원팀 ID는 K02 입니다.
select * from player 
	where player_name like '고%'
	and height >= 170
	and team_id = 'K02'; 