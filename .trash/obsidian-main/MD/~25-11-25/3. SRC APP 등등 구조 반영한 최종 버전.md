실제 프로젝트 구조에 맞게 전략 문서 수정 중입니다.


[1 tool called]


실제 프로젝트 구조에 맞게 전략 문서 수정 중입니다.


[16 tools called]


전략 문서를 실제 프로젝트 구조에 맞게 수정했습니다.

## 주요 변경사항

### 1. 디렉토리 구조 변경
- `app/store/` → `src/store/`
- 실제 프로젝트 구조 반영

### 2. 에이전트 구조 변경
- 에이전트 5개 → 6개 (kESG, ESRS, GRI, Rewrite, Chatbot, Realtime)
- GPT 슬라이스 제거 (AI 로직은 `lib/ai/`로 분리)

### 3. 핵심 원칙 강조
- 단일 스토어 절대 유지
- 확장 가능성: 새 에이전트 추가 시 슬라이스만 추가
- 비즈니스 로직 분리: AI/LLM 로직은 `lib/ai/`에 위치

### 4. 파일 구조 반영
```
src/
├── app/          # 라우팅
├── components/   # UI 컴포넌트
├── store/        # Zustand store (단일)
├── lib/          # 비즈니스 로직 + AI
├── hooks/        # 커스텀 훅
└── styles/       # 스타일
```

### 5. 에이전트 슬라이스 예시
- kESG, Chatbot 슬라이스 예시 코드 추가
- `lib/ai/` 함수 호출 패턴 명시

전략 문서가 실제 프로젝트 구조와 요구사항에 맞게 업데이트되었습니다. 단일 스토어 원칙과 확장 가능성을 강조했습니다.