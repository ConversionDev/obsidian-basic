좋습니다! 루트로 이동하는 것이 맞습니다. 그럼 다음 작업을 진행하겠습니다.

[1 tool called]

알겠습니다! `.env` 파일을 루트로 이동하는 것으로 결정했습니다.

## 📋 다음 작업 순서

### ✅ Step 1: `.env` 파일을 루트로 이동 (수동 작업 필요)

**현재 위치:** `services/authservice/.env`  
**이동 위치:** `프로젝트 루트/.env` (즉, `docker-compose.yaml`과 같은 위치)

**파일 내용 예시:**
```env
# 카카오 OAuth 설정
KAKAO_REST_API_KEY=your-actual-kakao-rest-api-key
KAKAO_REDIRECT_URI=http://localhost:3000/auth/kakao/callback
KAKAO_ADMIN_KEY=your-kakao-admin-key

# JWT 설정
JWT_SECRET=your-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm
```

---

### ⬜ Step 2: `docker-compose.yaml`에 환경 변수 전달 설정

**수정 위치:** `docker-compose.yaml`의 `authservice` 섹션

**추가할 내용:**
```yaml
authservice:
  build:
    context: ./services/authservice
    dockerfile: Dockerfile
  container_name: authservice
  ports:
    - "8081:8081"
  networks:
    spring-network:
      aliases:
        - authservice.local
  environment:
    - KAKAO_REST_API_KEY=${KAKAO_REST_API_KEY}
    - KAKAO_REDIRECT_URI=${KAKAO_REDIRECT_URI}
    - JWT_SECRET=${JWT_SECRET}
```

---

### ⬜ Step 3: `JwtTokenProvider.java` 생성

**파일 위치:** `services/authservice/src/main/java/com/elianayesol/api/jwt/JwtTokenProvider.java`

**역할:**
- JWT 토큰 생성
- JWT 토큰 검증
- JWT에서 사용자 정보 추출

---

### ⬜ Step 4: `RestTemplateConfig.java` 생성

**파일 위치:** `services/authservice/src/main/java/com/elianayesol/api/config/RestTemplateConfig.java`

**역할:**
- RestTemplate Bean 생성 (카카오 API 호출용)

---

### ⬜ Step 5: `KakaoService.java` 생성

**파일 위치:** `services/authservice/src/main/java/com/elianayesol/api/kakao/KakaoService.java`

**역할:**
- 카카오 액세스 토큰 요청
- 카카오 사용자 정보 조회

---

### ⬜ Step 6: DTO 클래스 생성

**파일 1:** `services/authservice/src/main/java/com/elianayesol/api/kakao/dto/KakaoTokenResponse.java`  
**파일 2:** `services/authservice/src/main/java/com/elianayesol/api/kakao/dto/KakaoUserInfo.java`

---

### ⬜ Step 7: `KakaoController.java` 수정

**파일 위치:** `services/authservice/src/main/java/com/elianayesol/api/kakao/KakaoController.java`

**변경 내용:**
- 더미 응답 제거
- 실제 카카오 인증 로직 구현

---

## 🎯 지금 해야 할 것

**1. `.env` 파일을 `services/authservice/.env`에서 프로젝트 루트로 이동** (수동 작업)

이동이 완료되면 **Step 2부터 제가 도와드리겠습니다!**

`.env` 파일 이동이 완료되었나요?