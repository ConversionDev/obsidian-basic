- 심볼릭 링크 : 한 파일이나 디렉토리를 가리키는 "바로가기 파일"
- Junction Link로 예방
- Unsloth가 잘못된 위치에 생성해도 자동으로 리소스 매니저로 연결
- => 작업 디렉토리
- 기존에 모델이 있으면 덮어쓰기 원래는 v1, v2 이런식으로 관리하고 최종 bestmodel을 두는 것이 좋음
- 배치 크기가 클수록 빠름 -> GPU 메모리 증가
- 배치 크기 작을 수록 느림 -> GPU 메모리 감소 
- 